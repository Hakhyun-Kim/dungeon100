# 백층 던전 (Dungeon 100)

매판 새로 생성되는 던전을 뚫고 100층까지 내려가는 **탑다운 3D 로그라이크** (three.js + react-three-fiber). 모바일 우선 웹앱 (React + Vite + TypeScript).
**공개 저장소** — 시크릿·내부 경로·**제출(행사) 관련 사항 커밋 금지** (2026-07-22 분리): 제출 체크리스트·소개/기술 문서·촬영 가이드는 비공개 저장소 **[dungeon100-submission](https://github.com/Hakhyun-Kim/dungeon100-submission)** 에서만 관리하고, 이 저장소의 문서·README에는 제출 로지스틱스를 적지 않는다.
설계·로드맵: `docs/DESIGN.md` (주차별 계획 포함 — 작업 완료 시 체크 갱신).
**대외 기술 문서: `docs/AI_DEVELOPMENT.md`** (2026-07-28 신설 — 공개 저장소용 「AI 활용 기술 문서」. README 최상단에서 한 번의 링크로 닿는다.
외부 독자(처음 보는 사람·심사자)가 읽는 유일한 기술 문서이므로 **대회·제출 로지스틱스는 절대 넣지 않는다** — 방법론·검증 장치·실측치만.
검증 하네스·봇·자동화·리소스 규칙을 크게 바꾸면 이 문서의 해당 절과 수치(커밋 수·층 수·레시피 수)도 같이 갱신할 것.)

## 이 문서 읽는 법

| 섹션 | 언제 보나 |
|---|---|
| [다음 후보](#다음-후보-우선순위) | **무엇을 할지** 고를 때 — 우선순위와 각 후보의 실측 근거 |
| [실행](#실행) | 개발·디버그·자동 검증 **명령과 훅** (검증 원칙·헤드리스 함정 포함) |
| [구조](#구조) | 파일이 어디에 뭐가 있는지 — **코드를 고치기 전에** |
| [원칙](#원칙) | 리소스·아키텍처·커밋 **규칙** |
| [완료 노트](#완료-노트-최근-순) | **왜 이렇게 만들었는지** — 새 작업 전에 관련 날짜를 훑을 것 |

**문서 관리**: 작업을 마칠 때마다 이 문서(구조·규칙 변경분)와 `docs/DESIGN.md`(체크리스트·시스템 설명)를
갱신한다. "다음 후보"는 항상 최신으로 — 끝낸 항목은 지우고 새 아이디어를 우선순위와 함께 넣는다.
큰 작업은 완료 노트에 **날짜 소제목 + 왜 그렇게 했는지**를 남긴다(무엇을 했는지만 적으면 다음 세션이 같은 함정을 다시 밟는다).

## 다음 후보 (우선순위)

> 게임 확장의 판단 기준 3가지(절차 생성 우선·재미·검증 가능)와 후보별 평가는 `docs/DESIGN.md`의 **"확장 방향"** 섹션 참조.

1. **밸런스 시뮬레이터 후속** — 2026-07-19 스노볼 제어까지 완료. 실측 이력: 파밍런(2층마다 전설)이 160아이템으로 61층+ 무저항 순항(적 스탯 캡이 15~21층에 걸린 뒤 아이템만 복리) → **깊은 층 위협 램프**(30층+ 받는 피해 가속·스폰 캡 7→9) 적용 후 **사망 10·10·15·30층 + 스노볼 관통 1/5(61층+, 159아이템)** — 최상급 파밍 가정에서도 가끔만 뚫리는 분포로 정착. 하드런(6·6·9·9·10·10, 평균 8.3)은 15층 미만이라 영향 없음. 잔여: 실플레이(상자 실제 개봉) 기준 표본, 50층+ 보스 표본. 교착 진단은 stuck 기록 시 localStorage `d100sim-stalltrace`(60초+ 체류 샘플) 참조.
2. **실기기 QA 패스** — iOS Safari(오디오 잠금·성능·auto 판정)와 자동 시연 전체 재생을 실제 폰에서 확인. 헤드리스로 검증 불가한 마지막 구멍. **2026-07-27 추가 항목**: ①카메라 셰이크를 걷어낸 뒤 멀미가 실제로 가셨는지(체감은 사람만 잴 수 있다) ②🔥 붉은 포털을 **모르고 지나치지 않는지**(발견율이 낮으면 나침반 화살표를 하나 더 다는 게 다음 수). **2026-07-29 추가**: ③찰나(❄️)·결의(💨) 두 버튼이 겹쳐 보이지 않는지·오조작 없이 구분되는지(둘 다 헤드리스에서 라이브 탭 검증을 못 했다).
3. **몬스터 아레나 난이도 상향** — **실측(2026-07-25): 클리어율 11/11 = 100%** (`PROFILE=human MODE=monster MAXFLOOR=12`, 사람형 봇). **2026-07-29 재확인: 16/16 = 100%**(캡 8·2판 — 이번엔 봇이 💨 결의까지 쓰는, 더 쉬워진 조건). 1~12층 아레나는 위협이 되지 않는다 → aliveCap/enemyMaxHp/touchDmg 층별 곡선 상향이 필요. 보석 근처 스폰 몰림 방지, 타입 분포 비율도 함께. 처치가 코인에 반영 안 됨(파밍 방지)은 의도된 것.
   - ⚠️ **주의: 임시 버프의 기여도는 아직 모른다.** 버프 도입 *전* 클리어율을 안 재 놨기 때문에 100%가 버프 때문인지 원래 쉬웠는지 구분할 수 없다. 다만 이미 천장이라 "아레나가 너무 쉽다"는 결론은 원인과 무관하게 성립한다.
   - 측정 범위 한계: 캡 12라 **얕은 층 아레나만** 쟀다. 아레나 강도는 층에 비례(aliveCap = min(18, 10+층×0.5))하므로 깊은 층은 별도 측정이 필요하다.
   - 봇은 회피가 정확하고 자동 조준이 붙으니 사람보다 유리할 수 있다 — 그래도 11전 무패는 조정 근거로 충분하다.
4. **숨은 보물방(벽 뒤 골방 + 보물)** — 두 갈래 틈(2026-07-22 완료)의 자매 아이디어 잔여분. 벽 허물기 or 틈 한쪽이 고립 골방으로 이어지는 변형.
5. **모바일 성능 추가 최적화** — 인스턴싱 정리 등. (⚡가벼움/✨고품질 토글·DPR 캡·블룸 폴백은 2026-07-22 완료 — 아래 완료 노트)
6. **저주 단계·무한 모드(101층+)** — 회차 도전 수정자와 스코어 러시. 추후. **2026-07-29**: 정교화 아이디어(「믿을 수 없는 화면」 지각 왜곡)의 설계 스케치를 `docs/DESIGN.md`의 "저주 단계 — 「믿을 수 없는 화면」 설계 스케치" 절에 남겨 놨다 — 상위 시스템(저주 단계 자체)이 없어 코드는 아직 없음. 다음 착수 시 그 절부터 읽을 것(최소 시험 단위 후보 3개 정리돼 있음).
7. **두 게임 통합** — 두 문 러너를 정식 모드로 흡수. 추후 검토.
8. **휘발되는 장 사람형 실측** (2026-07-29) — `PROFILE=human`으로 픽률(얼마나 자주 뽑히는지)·뽑았을 때 사람형 봇의 점수화(`scoreCard`)가 legendary 취급하는 게 적절한지·만료 순간 체감(대폭 강해졌다가 훅 약해지는 게 급사망으로 이어지지 않는지)을 재야 한다. 하드런은 표본이 적어 이번 세션엔 뽑힌 사례 자체가 안 보였다.
9. **몬스터 하우스 임시 강화 라이브 검증** (2026-07-29 남은 마지막 미검증 인터랙션) — 07-27에 넣은 「이번 층 한정」 버프는 **코인 무더기 3개를 몸으로 주워야** 화면이 뜨는데, 봇은 코인을 목표로 삼지 않아(출구·상자만 본다) 이 경로를 한 번도 안 밟는다. 사람형 봇에 "몬스터 하우스 층이면 `orbWorlds`를 경유지로 넣는다"를 추가하면 결의·찰나와 같은 방식(`.card` 클릭은 이미 드래프트 규칙이 처리)으로 실측된다.

## 실행
- `npm install` — 최초 1회
- `npm run dev` — 개발 서버 (기본 포트 5175, `PORT` 환경변수로 변경 가능)
- `npm run typecheck` — 타입 검사
- `npm run build` — 프로덕션 빌드
- **자동화 스크립트** (`scripts/`, Playwright — 로컬은 설치된 Chrome 재사용): `npm run floor-check`(층 생성 불변식 — 브라우저 없이 1초, 지형 생성기를 건드렸으면 반드시), `npm run assets-check`(리소스 규칙 — 런타임 리소스·외부 호스트·의존성 라이선스 vs `docs/assets.json` 원장, 브라우저 없이 1초 / `npm run assets-write`는 원장에서 ASSETS.md 표를 다시 씀), `npm run smoke`(부팅 스모크 — 배포 게이트와 동일), `npm run daily-ghost`(오늘의 던전 사서 실주행 → public/ghost/날짜.json, 멱등), `RUNS=n npm run balance-regression`(하드런 분포 vs 기준선), `npm run journal-data`(journal.html 데이터 블록·통계 타일 재생성 — ⚠️ **2026-08-01 이후 실행 금지**: 07-24~08-01 구간이 스쿼시 커밋 하나로 합쳐져 다시 돌리면 일별 차트가 그날 하나로 뭉개진다. 현재 journal.html에 구워진 데이터가 정확한 기록이므로 그대로 둘 것). 워크플로: deploy-pages.yml(푸시마다 typecheck→**리소스 규칙**→build→**스모크**→배포), daily-ghost.yml(00:15 KST 크론 — 실주행 커밋+직접 배포), balance-night.yml(03:30 KST 크론 — 드리프트 시 이슈 자동 생성).
- **디버그 층 이동: Shift+D** (Esc 닫기) — 빠른 버튼(1·5·10·…·100층) + 직접 입력, 이동 시 체력 회복. DEV에서는 항상, **배포판에서는 `?debug` 쿼리 필요** (예: …/dungeon100/?debug — 일반 방문자 오작동 방지 게이트, 2026-07-13). `e.code` 기반이라 한/영 입력 상태 무관. 일반 `d`는 이동 키라 Shift 조합 사용, 이동 키는 Shift 조합 무시.
- **디버그 보물: Shift+P** (run phase에서만, 같은 `?debug` 게이트) — 보물방 클리어(전설)와 동일 보상: 랜덤 아이템 3개 + 완전 회복 + 황금 잔광·전설음. 흐름 끊김 없이 즉시 지급(quiz/memory 화면 안 거침), 누를 때마다 다른 아이템(grantRewards에 Math.random 시드 전달).
- **디버그 코인: Shift+M** (같은 `?debug` 게이트, phase 무관) — 코인 +100. 대장간(메타 강화) 테스트용, 코인은 localStorage라 유지됨.
- **자동 시연 (상시 노출, 약 2분)** — 타이틀 세로 메뉴 네 번째 "🎬 자동 시연 보기" 버튼 (2026-07-24 ⚡빠른 시작이 2번째로 들어오며 한 칸 밀림). **2026-07-19 `?demo` 게이트 해제** — **이 시연이 최신 콘텐츠를 계속 보여 주는 상시 쇼케이스** 역할(README도 이 버튼을 안내). 타이틀 기본 포커스는 "모험 시작"이라 Enter로 시연이 잘못 시작될 일은 없음. 투어: 마을 산책 → **촌장 대화(가짜 선택 개그 "다른 방법은 없나요?"→"없어.")** → 1층 전투 → 보물 3연속(16종·희귀도·시너지) → **낡은 제단(수락까지 시연 — 체력 바치고 보물 획득)** → 두 문 달리기 → 몬스터 아레나 → **몬스터 하우스(층=시드라 배치가 결정적 → `findFloor`로 있는 층을 찾아 점프)** → **찢어진 페이지(수락 = 2개 층 건너뛰기)** → 45·75층(테마·안개) → **56층 소녀 찻자리(공주 장면 — goTown(girlScript(false))로 직접 진입, girlMet 저장 안 함 = 관람자 세이브 오염 방지)** → 85층 폐허 마을 → 30층 보스 → 일일 던전 예고 → 끝 화면(다시 보기/직접 플레이). 시나리오 드라이버(`src/demo/useDemoDriver.ts`)가 합성 키보드 이벤트(useMoveInput/useSteer가 window 키 이벤트 기반, `code`만 채워 표준 메뉴의 `e.key` 핸들러와 충돌 없음)로 걷고, 장면 전환은 App이 넘겨준 조작 프리미티브(DemoActions — village/dungeon/jump/treasure/altar/secretDoor 등) 직접 호출 — DEV 훅 불사용이라 배포판에서도 동작. 오버레이는 1.5초 보여주고 자동으로 넘김(포털·마을 문·방 이벤트는 거절), 명시적으로 수락해야 하는 장면은 `clickChoice`/`clickPrimary`로 직접 클릭. 자막 `.demo-caption`, 종료 버튼, 끝 화면 `.demo-end`. **세이브 격리**(2026-07-19): 시연은 실제 게임을 그대로 플레이하므로 시작 시 `suspendPersistence(true)`(`src/lib/store.ts`)로 **localStorage 쓰기를 끄고**(읽기·React 상태는 정상 → 시연은 자연스럽게 진행), 나가는 모든 경로를 **리로드로 통일**해 원래 세이브·화면으로 복귀한다 — 종료 버튼·"직접 플레이"는 그냥 리로드, "다시 보기"는 `d100-demo-auto` 세션 키를 심고 리로드해 자동 재생(처음부터). 그래서 시연 중 얻은 코인·기억·최고 층·일일 기록이 세이브에 남지 않고, 시연 뒤 일반 플레이는 저장이 정상 복구된다(모듈 플래그가 리로드로 초기화). 음소거(`d100-muted`)는 사용자 설정이라 sound.ts가 직접 저장 — 격리 대상 아님. **주의: 헤드리스(숨김 탭)에서는 크롬 집중 스로틀링으로 드라이버의 setTimeout이 죽어 시연 전체 재생 검증이 불가** — 개별 장면은 MessageChannel 틱 + `__pump`로 검증할 것.
- **디버그 모드에서는 전체 화면 플래시·펄스 오버레이 끔** (피격 hit-flash·보물 gold·저체력 low-hp — 반복 테스트 시 눈 피로, 2026-07-17). 소리 신호(심장박동 등)는 유지. 일반 플레이(비디버그)에는 그대로.
- **표준 메뉴 선택 UI** (`src/ui/Menu.tsx` — ChoiceList·PrimaryButton, 2026-07-19 표준화): 모든 선택 화면에서 **항상 한 항목이 하이라이트**(.focused 금색 테두리, 기본 = 첫 활성 항목)되어 있고, **방향키로 이동**(순환·비활성 스킵), **Enter/Space로 확정**, 숫자 1~4는 바로 선택, 마우스 호버는 하이라이트와 동기화. 진행 버튼(유일 버튼)·대사는 Enter/Space/→. 메뉴가 겹치면(디버그 창·시연 끝 화면) **나중에 열린 메뉴만 키를 받음**(모듈 레벨 메뉴 스택 — DebugPanel은 useMenuStack만 등록해 아래 메뉴를 잠금). 자동 반복(e.repeat) 무시 — 이동 키를 누른 채 화면이 떠도 오작동 없음.
- HUD는 z-index 50으로 오버레이(30) 위 — 음소거(🔊) 버튼이 팝업 중에도 항상 눌린다.
- 숨김 탭(헤드리스 프리뷰)에서는 크롬이 rAF를 멈춰 3D가 안 그려짐 — `?rafshim` 쿼리로 우회 (index.html의 개발용 심: 타이머 rAF + ResizeObserver 폴리필 + **`window.__pump(n)` 동기 프레임 구동** + **완전 헤드리스 페인(뷰포트 0×0)이면 getBoundingClientRect를 1280×800 가상 뷰포트로 폴백** — 없으면 r3f가 캔버스 크기를 못 재 영영 부팅 불가, 2026-07-19). 크롬 집중 스로틀링(오래 숨겨진 탭, 타이머 분당 1회)에서는 타이머가 다 죽으므로 자동 검증은 `__pump` + `__d100fixdt`(고정 dt) + MessageChannel 틱(스로틀 안 됨)으로 구동할 것. 클릭 후 React 렌더는 태스크 경계가 필요 — 같은 evaluate 안에서는 MessageChannel 왕복(tick) 후 DOM을 읽어야 함. DEV 훅: `__d100`(teleport/state/hitBoss/killEnemies/**ripple(power,color)** — state에 밸런스 봇용 `bossWorld`/`eshots`/`enemiesPos` 좌표와 🔥 갈림길 `forkWorld` 노출. ripple은 전투 없이도 임팩트 파문을 한 발 터뜨려 `__d100shot()` 픽셀 대조로 연출을 검증하려고 둔 것), `__d100run`(place/state), `__d100arena`(place/state/collect/hurt — 몬스터 아레나), `__d100town`(place/state — 걸어다니는 마을), `__d100app`(jump — 층 점프), **`__d100shot()`**(2026-07-26 — 렌더된 캔버스를 PNG data URL로 회수해 **AI가 실제 픽셀을 보고** 블룸·색·대비를 판단. `?debug`/DEV에서만 설치되고, 짝이 되는 `preserveDrawingBuffer`도 그때만 켠다 — 상시 켜면 일반 플레이에 렌더 비용이 붙는다. r3f는 className을 컨테이너 div에 붙이므로 캔버스 선택은 `.canvas canvas`, 실측 1600×900 PNG 회수 확인). **밸런스 봇 실측 예시**(2026-07-18): 회피 봇(링 유지+탄막 수직 회피, 8방향 키 합성)으로 10층 보스 1:1 → 12아이템 빌드 기준 6.3초 접전 끝 봇 패배(보스 HP 14% 남음) — 시뮬레이션은 로컬 DEV 전용, 프로덕션 번들엔 미포함. r3f 부팅은 클릭 루프보다 느릴 수 있으니 `window.__d100` 등장까지 넉넉히 대기할 것. (아레나 훅은 씬 마운트 직후 passive effect라 같은 evaluate 안에서 tick 몇 번 돌려야 등장.)
- ⚠️ **검증 원칙 — 전투·밸런스를 잴 때 순간이동(`__d100.teleport`) 금지** (2026-07-26): 무리 한가운데나 출구 위로 텔레포트하면 **사람이 절대 만들 수 없는 상황**이 만들어져 결과가 망가진다(실측: 「마지막 문단」 검증에서 출구 위로 순간이동시켰더니 5초를 못 버티고 죽어, 메커니즘이 아니라 텔레포트가 측정된 꼴이 됐다). 전투가 얽힌 검증은 **시뮬봇처럼 실제로 걸어서** 접근할 것. 예외는 ①마을(적 없음 — 헤드리스 캔버스 미측정 워크어라운드) ②1~7층 UI 흐름 스모크(「마지막 문단」 밖, 적도 옅음 — smoke.mjs가 층 번호를 단언해 지킨다).
- **밸런스 자동 시뮬레이터** (`src/dev/simBot.ts`, DEV 전용 — 프로덕션 번들 미포함): localhost에서 콘솔에 `__d100sim.start({ runs: 5 })` — 봇이 1층부터 자동 플레이(BFS 길찾기·탄막 회피·두 문 달리기 정답 조향·포털/드래프트/마을 자동 진행, 5층 마을 문은 거절해 체크포인트 비오염) 후 **사망 층 분포 리포트**(console.table + `__d100sim.report` + localStorage `d100sim-report`). 옵션: `maxFloor`(층 캡), `chestEvery: n`(n층마다 전설 보상 = 파밍런 가정, Shift+P 재사용), `daily: true`(타이틀 「오늘의 던전」으로 입장 — 사서 실주행 파이프라인용, 2026-07-24), `stop()`/`status()`.
  - **프로필 2종**(2026-07-25) — `profile: 'hard' | 'human'` (기본 hard).
    - `hard` — **기존 기준선 봇. 동작을 절대 바꾸지 말 것** (`balance-baseline.json`이 이 프로필로 잡혀 있다). 출구로 직행, 드래프트 무작위, 방 이벤트 전부 거절, 보상은 즉시 수령.
    - `human` — **사람이 화면에서 보는 것만 읽고 사람처럼 판단한다**(봇에게 특별한 정보를 더 주지 않는 게 원칙): ①**보물상자를 먼저 들르고** 출구는 그다음(`pathGoal: 'chest'`) ②드래프트를 `.evo-hint`(합본까지 N장)·희귀도 클래스·`.hp-text` 체력비로 점수화해 선택(`scoreCard` — 합본 카드 > 「집으면 완성!」 > 남은 장수 적은 순 > 희귀도, 저체력이면 생존 태그 가산, 동점은 난수로 흔들어 판마다 빌드가 갈리게) ③방 이벤트를 상황으로 판단(`decideRoomEvent` — 제단은 체력 55%+일 때, **두 갈래 틈은 BFS로 재서 출구가 실제로 3칸+ 가까워질 때만**, **무너지는 서가는 출구까지 BFS 거리 ÷ 속도가 11초 미만일 때만**, 찢어진 페이지는 보상 손실이라 거절) ④두 문 달리기는 3문 완주(전설)까지 밀어붙임 ⑤저체력이면 회피 가중치 ↑·보스 링 7까지 후퇴 ⑥**역류(`.rush-bar-wrap`)가 뜨면 상자고 뭐고 버리고 출구로**.
  - **몬스터 모드**(2026-07-25) — `mode: 'kids' | 'adult' | 'monster'`. monster면 마을 입구에서 「몬스터 던전」을 골라 보물상자가 **몬스터 아레나**로 열린다. `arenaBrain`이 가장 가까운 남은 보석으로 직행하며 던전과 같은 규칙으로 탄막 수직 회피·무리 밀림·경계 클램프를 한다. 쓰러지면 `arenaRetry`(기본 2)까지 재도전 후 모은 보석만 받고 나감(재도전 카운터는 상자마다 독립). 리포트에 `arena: {tries, cleared, failed, clearRate}`. GemArenaScene DEV 훅에 `enemiesPos`/`eshots`/`radius`를 노출해 던전과 같은 형식으로 읽는다.
  - **발밑 두 버튼 실제 탭**(2026-07-29, human 전용) — 봇이 ❄️ 찰나·💨 결의를 **실제 DOM 버튼에 pointerdown을 쏘아** 발동시킨다(훅이 `click`이 아니라 `pointerdown`을 듣기 때문에 `btn.click()`으로는 절대 안 눌린다). 찰나는 둘러싸였을 때(반경 4.5 안 적 3+ · 보스 7 이내 · 다가오는 탄 2발+), 결의는 맞기 직전(가장 가까운 적 1.8 이내, 저체력이면 3.2)이나 역류·「마지막 문단」 중에 쓴다. 리포트 `abilities.{freeze,resolve}`에 **충전(게이지 상승 에지)·탭·발동(씬 active 상승 에지)·버튼 경로 수·발동 중 피해 횟수**가 남는다 — 결의의 `hpLossWhileActive`가 0이 아니면 무적 가드가 샌 것이다. ⚠️ 아레나 중에는 `.resolve-btn`이 **둘**(정지된 DungeonScene 것 + 아레나 것) 존재하므로 봇은 전부에 쏜다(정지된 쪽은 훅 안에서 pausedRef로 스스로 무시).
  - `PROFILE=human MODE=monster RUNS=3 MAXFLOOR=12 npm run balance-regression`
  - `PROFILE=human RUNS=3 MAXFLOOR=12 npm run balance-regression` — 사람형 분포를 **참고 지표로만** 측정(기준선 비교 건너뜀, 단 stuck은 프로필 무관 결함이라 실패 처리). 리포트에 `profile`·`maxFloor`·`avgItems` 포함. 판당 예산은 프로필별(hard 15분 / human 25분)이고, 예산 초과 시 봇의 마지막 층·완료 판수를 찍는다 — **'교착'과 '그냥 깊이 내려감'을 로그만 보고 가릴 수 있게**(실측에서 실제로 오진할 뻔했다: 40분 예산을 넘긴 이유가 봇이 22·23·24층으로 계속 전진 중이어서였다).
  - **실측 (2026-07-25)**: 하드런은 6·9층 사망인데 사람형은 **캡 12에서 3판 전부 무사(아이템 46.7)**, 캡을 25로 올려도 **3판 전부 무사·stuck 0·평균 아이템 94.7**. 즉 기존 기준선(중앙값 5~13)은 *게임의 절반만 플레이한 난이도*다 — 회귀 탐지용으로는 여전히 유효하지만 "이 게임이 실제로 어렵나?"에 답할 땐 사람형 수치를 봐야 한다.
    - **캡 50 검증 결과(2026-07-25): 램프가 안 문다.** 2판 전부 50층 캡 도달·사망 0·stuck 0·**평균 아이템 195.5**. 30층+ 위협 램프를 20층이나 지나고도 위협이 되지 않았다.
    - **원인 — 성장은 복리, 위협은 선형.** 일반 적 HP는 `18 + 층×7`로 **선형**(50층 368)인데 주인공 DPS는 곱연산으로 쌓인다(공격력 ×1.18/장·연사 ×1.14/장·멀티샷 +1). 위협 램프(`threatFloor = 층 + max(0,층-30)×0.6`)는 **받는 피해만** 가속할 뿐 적 HP는 안 올려서, 적을 여전히 순삭하며 층을 빠르게 지난다. 게다가 받는 피해 쪽도 방어(캡 50%)·회피(캡 45%)·흡혈·최대 체력(+25/장)이 함께 쌓여 상쇄된다.
    - **튜닝 모델이 실제 수급을 과소평가했다.** 램프는 '파밍런 가정'(2층마다 전설 = 61층에 159아이템 ≈ **층당 2.6개**)으로 맞췄는데, 사람형 실플레이는 **층당 3.9개**(50층 195.5)다 — 최악 가정보다 1.5배 빠르다. 상자를 매 층 3문 완주(전설 3개)로 여는 것이 파밍 가정보다 후하기 때문.
    - **조정 방향 후보**(택일 아님, 조합 가능): ①적 HP/피해도 깊은 층에서 초선형으로(램프를 '받는 피해'가 아니라 '적 스탯'에 걸기) ②공격력·연사에도 소프트 캡(이동 속도·사거리처럼) ③층당 아이템 유입 자체를 제한(상자 보상 축소 또는 드래프트 격층).
    - **동굴 층 주행 확인**: 12·16·18층(동굴)을 3판 모두 통과, 층당 체류가 인접 방-복도 층(13·15·17·19)과 같은 범위 — 교착·비정상 지연 없음. floor-check(정적)로만 알던 연결성이 실제 주행으로 뒷받침됐다.
    - **CI 예산 실측**: 사람형 25층 캡이 **판당 약 17분**(SwiftShader 헤드리스 + lite). 야간에 넣는다면 캡 25·2판이 현실적인 상한.
  - ⚠️ **사람형 봇 튜닝 시 주의** — 저체력 보스 후퇴처럼 '조심하는' 행동은 기존 교착 복구(보스 HP가 안 깎이면 링 축소 → bossPathMode로 파고들기)를 덮어쓸 수 있다. 던전에는 회복 수단이 없어 무한정 물러나면 체력은 그대로인 채 층 예산만 태우고 stuck이 난다(실측: 10층 보스 3판 중 1판). 그래서 후퇴에는 반드시 **시한**(층 진입 90초)을 둔다. 층 캡·멈춤 복구는 sessionStorage 저장 후 자동 리로드로 이어짐. 헤드리스(?rafshim)에서도 돌지만 **보이는 탭이 안정적**(숨김 탭은 캔버스 미측정 300×150 시 resize 이벤트로 재측정 강제 — 봇에 내장). 봇 좌표는 `__d100.state()`의 `bossWorld`/`eshots`/`enemiesPos`, 지형은 `__d100.grid()`.
- 배포: main 푸시 → `.github/workflows/deploy-pages.yml` → https://hakhyun-kim.github.io/dungeon100/
- **개발 일지**: `public/journal.html` — 정적 단일 파일(빌드 없이 Vite `public/`가 그대로 `dist/` 루트로 복사), 배포판에서 https://hakhyun-kim.github.io/dungeon100/journal.html 로 접근. git log(58커밋, 2026-07-22 갱신) + 실제 대화 인용 기반 SVG 차트 3종(일별 증감 다이버징 바·누적 성장 곡선·카테고리 랭킹) + 초기/후기 프롬프트 비교. 게임 세계관 팔레트(잉크빛 보라·금빛·장미) 재사용, 상단 네비 "← 백층 던전으로" + 우측 🌗 다크모드 토글(`data-theme`, localStorage `d100-journal-theme`). 데이터 갱신은 **`npm run journal-data`** (2026-07-24 자동화 — `[journal-data]` 마커 블록 days/cum + 통계 타일·본문 커밋 수·메타 날짜 자동, milestones/categories만 수동 큐레이션). ⚠️ **2026-08-01 히스토리 정리(07-24~08-01 스쿼시) 이후로는 실행 금지** — 구워진 데이터가 정확한 기록이다(위 「실행」 절 경고 참조).

## 구조

**기능별 1파일 원칙** — 오버레이·화면은 `src/ui/`에 기능 단위로 분리되어 있어 커밋도 기능 단위로 나눌 수 있다. 표시(JSX)는 컴포넌트가, 상태 전이·보상 로직·효과음은 App 콜백이 담당. **주의: 시뮬봇(simBot)·시연 드라이버가 DOM 클래스(.choice-btn/.dialog-choices/.big-btn/.card/.village-action 등)를 셀렉터로 쓰므로 클래스명은 바꾸지 말 것.**

- `src/App.tsx` — phase 상태 머신 + 배선(≈900줄): `title` → `story`(인트로, 최초 1회·건너뛰기 가능) → `village`(**걸어다니는 3D 마을** — 던전 입장 전·5층 방문·부활 공용) → `run` → **보물상자 미니게임(모드에 따라 갈림)**: 수학 모드 → `doorrun`(두 문 달리기, 최대 3라운드 푸시-유어-럭) / 몬스터 모드 → `arena`(몬스터 무리+보석 3개) → `quiz`(결과/계속 선택) → **포털에 몸을 넣으면 곧장** `draft`(보상 3택 1) → `run` / `over`(사망 → 재도전 or 마을). 사망 판정은 hp useEffect. Canvas는 run 계열 phase에만 마운트(배경·fog는 Canvas 레벨 — **마을은 시절 색, 던전은 10층 테마 색(dungeonTheme(floorNo).bg), 던전 안개는 깊을수록 짙어짐**(near 20→11·far 44→28 선형, 미로 난이도 램프; 미니게임·마을은 기본 20/44)), 층 전환은 DungeonScene key 리마운트, 미니게임 라운드는 DoorRunScene/GemArenaScene key 리마운트. 미니게임 결과는 quizResultRef(seq 증가)로 던전 씬에 전달. 보상: 통과한 문·주운 보석 수 = 아이템 수, 3개 완주 = 전설(3개+완전회복), 실패 = 전부 빈손. **보물상자 모드 분기**는 modeRef(onChest useCallback 안 stale 방지)로 판단.
- `src/ui/` — **오버레이·HUD 컴포넌트 (기능별 1파일)**:
  - `Menu.tsx` — 표준 메뉴 선택(ChoiceList·PrimaryButton·useMenuStack). 위 "표준 메뉴 선택 UI" 참조.
  - `Hud.tsx` — GameHud(층·체력·처치·코인)/ArenaHud/VillageHud/BuildRow(빌드 칩)/BossBar.
  - `TitleScreen.tsx` / `StoryScreen.tsx`(인트로+책 퀴즈) / `EndingScreen.tsx`(엔딩 선택·슬라이드·통계) / `OverScreen.tsx`(게임오버·부활 선택) / `DraftScreen.tsx`(3택 1).
  - `VillageOverlay.tsx`(걸어다니는 마을 DOM — 힌트·상호작용 버튼·대화창) / `TownDialogScreen.tsx`(별도 DOM 마을 화면 — 56층 소녀 찻자리) / `ShopScreen.tsx`(대장간).
  - `ArenaBuffScreen.tsx`(아레나 임시 버프 2택 1 — phase는 'arena' 유지, 오버레이만 얹음).
  - `FloorPrompts.tsx`(마을 문·제단·찢어진 페이지·두 갈래 틈·무너지는 서가 — **포털 확인 화면은 없다**, 2026-07-27) / `ChestScreens.tsx`(보물 결과·아레나 재도전) / `LoreScreens.tsx`(벽의 글귀·흔적·기억·기억 완성).
  - `DebugPanel.tsx`(Shift+D 층 이동) / `DemoOverlay.tsx`(시연 자막·종료·끝 화면).
- `src/lib/gaKeys.ts` — **GameAnalytics 키 설정 파일** (`GA_KEYS.gameKey`, `GA_KEYS.secretKey`). 키 직접 기입 또는 환경 변수 폴백.
- `src/lib/analytics.ts` — **GameAnalytics (GA) 텔레메트리 파사드** (`gaKeys.ts` / `VITE_GA_*`). 층 진행·드래프트·방 이벤트·보스전·능력 사용·대장간 트래킹. 봇/시연/스모크 자동 차단.
- `src/lib/arenaBuffs.ts` — **아레나 임시 버프** 10종(ARENA_BUFFS)·2택 뽑기(`pickArenaBuffs`)·적용(`applyArenaBuffs`). id는 `ab_` 접두사로 UPGRADES와 절대 안 겹치게 — 본체 build·진화 조합·도감에 섞이면 안 되는 값이다. **2026-07-27부터 몬스터 하우스도 같은 풀·같은 함수를 공유**(App의 `houseStatsRef` — 「이번 층 한정」으로 스코프만 다름, `ArenaBuffScreen`에 `variant` prop 추가).
- `src/lib/meta.ts` — 대장간 영구 강화 데이터·공식 (Meta·SHOP_ITEMS·shopCost·metaSpeed).
- `src/lib/ghost.ts` — **🤖 AI 사서 고스트** (2026-07-24). 시뮬봇 실측 분포로 보정한 추상 층별 시뮬레이션(순수 함수 `ghostRun(seed)` → 사망 층+사유) — 판마다 '먼저 읽은 AI'의 기록이 목표로 붙고, 일일 던전은 날짜 시드라 모두 같은 사서와 경쟁. 상수 변경 시 분포 재측정: DEV `__d100app.ghostDist(n)` (기준 p10 4·중앙 8·p90 13·최대 ~30).
- `src/lib/daily.ts` — **일일 던전** (todayKey·dailySeed·DailyRecord). 날짜 시드를 층 시드(`generateFloor(floorNo, seedOffset)`)·드래프트·문제 시드(runId 대신 dailyNum)에 섞어 모두가 같은 맵·같은 문제(어른 고정)에 도전. 기록 `d100-daily`, HUD 📅, 공유 카드에 날짜 라벨. 적 타입 구성 등 Math.random 부분은 비결정 — 기록 기준은 도달 층수.
- **방 이벤트** (dungeon.ts가 배치, DungeonScene이 상호작용, `src/ui/FloorPrompts.tsx`가 화면 — 2026-07-19): **제단**(3층+ 30%, 체력 30% 바치면 보물 1개 — 바치면 소멸·거절 시 재무장·체력 부족 시 비활성), **몬스터 하우스**(5층+ 22%, 흔적·소녀 층 제외 — 중간 방 스폰 2배+·바닥 붉은 기·코인 무더기 3개는 몸으로 줍기·탐욕 배율), **찢어진 페이지**(6~96 비보스층 22% — 2개 층 건너뛰기, 착지 충격 8%·건너뛴 보상 없음), **두 갈래 틈**(2026-07-22, 4층+ 25%, 소녀 층 제외 — 가장 멀리 떨어진 두 방에 틈 한 쌍, 들어가면 반대편으로 순간이동하는 지름길. 왕복 무제한·도착 자비 0.9s·거절/도착 후 두 틈 모두에서 벗어나야 재무장, phase `rift`·riftGoRef/riftRetryRef), **무너지는 서가**(2026-07-25, 7층+ 비보스 20% — 흔들면 보물 1개 즉시 + 16초 역류 카운트다운, 시간 안에 출구 도달 시 레어+ 보상·초과 시 낙석 피해. phase `collapse`·collapseUsedRef/collapseRetryRef, 역류 상태는 `rushRef`로 씬에 전달). 모든 rand 호출이 기존 배치 뒤라 예전 층 구조 보존. 시뮬봇·시연 드라이버는 자동 '그만둔다'(시연 declinePhases에 rift 포함). DEV state에 altarWorld/secretWorld/orbWorlds/riftWorlds.
- `src/demo/useDemoDriver.ts` — 자동 시연 시나리오 훅 — App은 조작 프리미티브(DemoActions)만 제공. 프로덕션 번들에 포함(관람용 상시 쇼케이스).
- **몬스터 아레나** (보물상자 '몬스터' 모드, `arena`/`arenaover` phase + `GemArenaScene`) — 수학 대신 우르르 몰려오는 무리를 뚫고 바닥의 보석 3개를 몸으로 주우면 능력치업(=3문 완주와 동일 보상). **아레나 전용 체력**(ARENA_MAX_HP=100, 본체와 분리)이 0이 되면 `arenaover`로 — 본체는 무사하고 **몇 번이고 재도전**(arenaTry로 리마운트) 또는 모은 보석만큼 받고 나가기(`bailArena`→grantRewards(N)). 아레나 HUD는 👹 + 아레나 체력바 + 💎 진행도. **보석 1·2개째를 주우면 「이번 아레나 한정」 임시 버프 2택 1**(ArenaBuffScreen, 선택 중 정지). DEV 훅 `__d100arena`(place/state/collect/hurt).
- `src/three/DungeonScene.tsx` — 층 하나의 씬+시뮬레이션. 지형·적·투사체·파티클 전부 InstancedMesh. **잉크 리빌**(2026-07-24): 지형이 ε 크기로 배치됐다가 조작 가능해진 첫 프레임부터 입구→먼 곳 순으로 자라난다(revealRef/writeReveal — 시각 전용, 충돌·봇 불변). **10층 단위 던전 테마**(DUNGEON_THEMES 10종 — 바닥 체커·벽·배경 팔레트: 보랏빛 서장→이끼→가을 잉크→푸른 밤→서리→장미 잉크(56층)→녹슨 철문→잿빛→핏빛→금빛 마지막 장; `dungeonTheme(floorNo)` export, App이 배경·안개색으로 재사용). 시뮬레이션은 useFrame에서 ref 기반(React 상태는 onDamage/onKill/onExit/onChest 이벤트만). 타격감: 피격 흰색 번쩍(인스턴스 색)+넉백+스파크, 처치 폭발, 히트스톱, **임팩트 파문**(`ripple(x,z,power,color)` — 맞은 자리에서 퍼지는 충격 링 인스턴스 14개. **카메라는 절대 흔들지 않는다**, 2026-07-27. DEV `__d100.ripple(power,color)`로 픽셀 확인 가능). **🔥 모험의 길 붉은 포털**(`fork` prop → `forkSpot(floor)` 자리, 접촉 반경 1.1 → `onExit(true)`). 파워업 시각화: 멀티샷 궤도 구슬, 공격력별 투사체 크기·색, 체력별 몸집, 보물 획득 황금 잔광. 입력은 useMoveInput 훅 (키보드 + 터치 드래그 — 터치·펜은 가상 조이스틱 시각화 표시, 캔버스 위·버튼 밖에서만 시작, 선택 인자 pausedRef가 true면 스틱 숨김). pausedRef로 퀴즈/드래프트/게임오버 중 정지. DEV 전용 `window.__d100`(teleport/state) — 자동 검증용. **찰나(시간 동결, 2026-07-27)**: 처치 6회 게이지 → 발밑 ❄️ 버튼(`useFreezeInput`, `.freeze-btn`, F키 대응)으로 1.1초 발동, 적 AI·보스·탄막 루프만 `{ const dt = frozenNow ? 0 : worldDt; ... }`로 감싸 그 구간의 dt를 가린다(플레이어 이동·차지 샷은 바깥 dt 그대로라 안 얼어붙음). 접촉 피해 판정 3곳에 `!frozenNow` 가드 필수(안 걸면 얼어붙은 적과 겹쳐 있는 동안 매 프레임 피해가 들어간다). `window.__d100.state().freeze`로 검증. **결의(무적 대시, 2026-07-29)**: 찰나와 짝을 이루는 두 번째 처치 게이지 — 방향이 반대다(찰나=세계를 멈춤/공격 리듬, 결의=짧게 무적으로 뚫고 지나감/회피 판단). 처치 4회 게이지 → 발밑 💨 버튼(`useResolveInput`, `.resolve-btn`, R키 대응)으로 0.4초 발동, 그 순간의 입력 방향(없으면 바라보는 방향)을 고정해 고속 돌진(`RESOLVE_DASH_SPEED`)하고 자동 발사가 멈춘다(차지 중 정지와 같은 `fireTimer` 억제 패턴). **가드는 찰나처럼 호출부 3곳에 따로 걸지 않고 `hurtPlayer` 함수 안 한 곳에만 뒀다** — 이 함수가 이미 모든 접촉 피해(적·수문장·탄막·좀먹은 바닥, 실측 4곳)의 유일한 진입점이라 호출부마다 가드를 걸면 새 피해 판정이 생길 때 빠뜨릴 위험이 있다. `window.__d100.state().resolve`로 검증. 두 게이지는 완전히 분리된 ref·훅·버튼·키(F/R)라 같은 처치가 동시에 둘 다 채워도 서로 무관하다. GemArenaScene도 같은 상수·훅(`RESOLVE_KILLS`/`RESOLVE_DASH_DURATION`/`RESOLVE_DASH_SPEED`/`useResolveInput`)을 export해 재사용 — 아레나는 찰나가 아예 없어서(이번에 처음 결의만 이식) 훅 재사용이 자연스러웠다. **좀먹은 바닥**: `floor.mire` 셀을 바닥 색 레이어에 어둡게 lerp해 눈에 보이는 함정으로 렌더, 칸당 1회 발동(`mireHit` ref).
- `src/lib/dungeon.ts` — 절차 생성. 방 흩뿌리기 + 폭 2 L자 복도 순차 연결(연결 보장). **`forkSpot(map)`**(2026-07-27, 순수·난수 0회) — 🔥 모험의 길 붉은 포털 자리를 출구에서 3~5칸 떨어진 설 수 있는 칸 중 결정적으로 고른다(다른 배치물과 3칸 이상 이격, floor-check가 검사). `GRID`=44셀, `CELL`=2. 충돌은 `canStand`(네 모서리 셀 검사). 시작 방 안전지대, 출구 인접 스폰 금지, 층당 보물상자 1개(시작·출구에서 떨어진 곳). **동굴 층**(2026-07-25, `isCaveFloor`/`carveCave`)은 이 방-복도 대신 셀룰러 오토마타로 지형을 깎는다 — 판정은 **별도 난수 스트림**이라 방-복도 층의 rand 소비 순서가 예전과 동일하고, 방은 '완전히 열린 사각형'만 잡아 아래 배치 로직이 그대로 통한다(생성기 확장 시 지켜야 할 규칙 4가지는 `docs/DESIGN.md`의 「절차 생성 확장의 안전 규칙」). **좀먹은 바닥**(2026-07-27, `mire` 필드): 5층+ 22%, 중간 방 하나의 내부 칸 중 ~32%를 함정으로 흩뿌린다 — 방 내부(전부 바닥)에만 놓여 이동을 막지 않으므로 floor-check의 도달 가능성 불변식과 원천적으로 무관(그래도 벽 속 배치·도달 가능성은 floor-check.mjs가 검사).
- `src/three/DoorRunScene.tsx` — **두 문 달리기 미니게임** (두 문 러너 인게임 재현). 자동 달리기 + 좌우 조작(useSteer: 화면 좌/우 꾹·←/→, 터치 기기엔 ◀▶ 존 인디케이터), 보라 게이트·유리 옆벽·문제판, 문 사이 벽 막힘, 몸으로 정답 문 통과 → onDone(true)·색종이 / 오답 💥 뒤로 넘어짐·정답 문 초록 표시 → onDone(false). 미니게임 동안 DungeonScene은 hidden(보임·카메라 양보). DEV 훅 `__d100run`(place/state), 시간 가속 `__d100speed`.
- `src/three/GemArenaScene.tsx` — **몬스터 아레나 미니게임** (보물상자 '몬스터' 모드). 오픈 아레나(반경 ARENA_R=9, 그리드 없이 경계 클램프) + 계속 몰려오는 무리(aliveCap까지 재스폰) + 고정 위치 보석 3개(octahedron). **무리는 1층부터 4타입 전부 섞임**(pickArenaType — chaser/shooter/dasher/tank, 본체 던전을 미리 맛보게 + 긴장감) — 타입별 AI·실루엣·슈터 탄막(eshots)까지 본체 이식. 본체와 동일한 useMoveInput·자동조준 발사·넉백(탱커 면역)·파티클 재사용. 보석 3개 완수 → onDone(true,3)·전설 보상, 체력 0 → onDone(false, 모은 보석 수). 아레나 동안 DungeonScene은 hidden(카메라 양보). DEV 훅 `__d100arena`.
- `src/three/TownScene.tsx` — **걸어다니는 3D 마을** (`village` phase). 옛날 RPG처럼 광장(우물·여관·대장간·촌장 집·나무·담장)을 useMoveInput으로 돌아다니며, NPC(👵촌장/👧니나/🧔무크)에 다가가면 하단에 상호작용 버튼 → 대화. **던전 입구 아치는 몸을 넣으면 묻지 않고 들어간다**(`ENTER_R` 1.25 — 감지 반경 2.3보다 훨씬 좁게 잡아 옆을 지나다 끌려 들어가지 않게, 벗어나면 2.8에서 재무장. 대화 중엔 미발동 = 닫자마자 다시 열리는 루프 방지. `onEnter` prop, 자동 시연은 동선을 직접 잡으므로 `autoEnter={!demoRunning}`으로 끈다, 2026-07-27). 새 도전(`enter`)일 때만 던전 종류 3택이 뜬다 — 그건 확인이 아니라 난이도 선택이라 없앨 수 없다. **시간의 흐름**(2026-07-18): 마을 모습이 층 깊이에 따라 변함 — 20층 단위 5개 '시절'(고요한 밤→가을바람→습격의 흔적→방벽의 시간→폐허와 새벽; 조명·팔레트·단풍/잎 진 나무·그을음·부서진 담장·판자 창·통나무 방벽·모닥불·잔해·새벽빛+꽃) + 같은 시절 안에서도 5층 단위 시드로 나무·잔해 재배치. 기능 앵커(건물·NPC·입구 위치, 충돌)는 고정. 새 도전 허브(enter ctx)는 항상 시절 0. `villageStage`/`VILLAGE_STAGE_NAMES`(HUD 칩)/`VILLAGE_STAGE_BG`(Canvas 배경·안개색) export, App이 `floorNo` prop 전달(key 리마운트). 씬은 근처 대상만 onNear로 App에 보고(이름표는 makeTextTexture 스프라이트). App이 대화창(`.village-talk`)·입구 선택지를 DOM으로 렌더. 상황(TownContext)에 따라 입구가 다르게: `enter`=난이도(모드) 선택→1층 / `visit`=현재 층 복귀 / `death`=체크포인트 층. 대화 중에만 이동 정지(villagePausedRef). 56층 소녀는 여전히 별도 DOM 화면(`town` phase). DEV 훅 `__d100town`(place/state).
- `src/three/Hero.tsx` — 공용 주인공 블록 캐릭터 (던전·미니게임·마을 세계관 통일). **내부 주스(2026-07-22)**: 월드 좌표 변화로 이동을 감지해 달릴 땐 팔다리 스윙+앞기울임+통통, 멈추면 숨쉬기 — 씬 코드는 그대로, 여기 한 곳만 고치면 전부 적용. **변신(2026-07-22)**: 던전 종류 선택에 따라 모습이 바뀜(`variant` prop — kids 초등학생: 노란 모자·빨간 책가방·반바지·0.84배 / adult 대학생: 후드티·청바지·갈색 책가방 / monster 모험가: 가죽옷·붉은 머리띠·등에 검) — "책이 주인공을 그 장에 맞는 모습으로 그린다". App이 mode를 DungeonScene·DoorRunScene·TownScene에 `heroVariant`로 전달, 아레나는 monster 고정. DEV 훅 `__d100app.mode(m)`로 변신 테스트.
- `src/three/fx.tsx` — **절차 생성 연출 텍스처·공용 이펙트 (2026-07-22 그래픽 품질 패스)**: 블롭 섀도우(방사형 캔버스 그라데이션 — `BlobShadow` 컴포넌트 + 인스턴스드용 `getBlobShadowTexture`), 돌바닥/벽 결(`getFloorTexture`/`getWallTexture` — 회색조 결 1장을 인스턴스·재질 색에 곱해 테마 10종 전부 커버, bump 겸용), 글로우. **포스트프로세싱**은 App.tsx Canvas의 `EffectComposer`(mipmapBlur Bloom 임계 0.58 + Vignette 0.52, multisampling 4) — emissive가 실제로 빛나며, 저사양 폴백은 로드맵 후보. 가짜 AO·타일 변주는 DungeonScene 지형 배치에서 인스턴스 색으로 굽는다(렌더 비용 0). 잉크 전환(`.ink-wipe`)은 App이 층 이동·사망·마을 진입 시 재생(pointer-events 없음 — 시뮬봇·시연 클릭 안 막음).
- `src/three/textTexture.ts` — 한글 텍스트 → 캔버스 텍스처 (문제판·문 답, 두문러너 labels.ts 축소판).
- `src/lib/quiz.ts` — 미니게임 문제 생성. **던전 종류(DungeonMode: 'kids' 초등 / 'adult' 어른 / 'monster' 몬스터)** × 층 깊이로 난이도 결정 — kids: 한자리±→두자리±→곱셈구구→두자리×한자리 / adult: 두자리±→두자리×한자리→혼합→두자리×두자리. 'monster'는 수학 대신 아레나 전투라 makeQuiz 미호출. 근접 오답, 문제 은행 없음. 라운드 보정 +6/라운드. 던전 입구(마을 choice)에서 모드 선택, HUD에 🎒/🧠/👹 표시.
- `src/lib/sound.ts` — Web Audio 합성 효과음 **28종** (파일 없음): tap/pick/hit(연사 제한 45ms)/**crit**/kill/hurt/doorrun/pass/crash/treasure/legend/memory/lore/portal/bell/gift/over/enter/heartbeat/roar/unlock/**chargeReady**/**blast(power)**/**countdown(urgent)**/**freezeReady**/**freeze**/**resolveReady**/**resolve**(2026-07-29 — 표가 낡아 찰나 2종이 빠져 있던 것도 이번에 같이 채움). phase 전환음은 App useEffect, 타격·통과음은 씬에서 직접 호출. 음소거: localStorage `d100-muted` + HUD·타이틀 🔊 버튼. `getAc()`로 AudioContext를 music.ts와 공유.
  - **마스터 버스**(`masterBus(c)` — 2026-07-26): 효과음·BGM이 **전부 한 GainNode(0.9) → DynamicsCompressor → destination**을 지난다. 예전엔 모두 destination 직결이라 연사 히트 + BGM + 보스 포효가 겹치면 합이 1을 넘어 지직거렸다. 개별 vol을 낮추는 대신 봉우리만 눌러 정리한다 (실측: 버스에 AnalyserNode를 물려 효과음·BGM 양쪽 RMS가 잡히는 것 확인).
  - **`noise(..., q)`** — 밴드패스 첨예도. 히트는 q 5·치명타 q 9로 좁고 날카롭게(연사해도 안 뭉개짐), 피격은 q 0.7로 넓고 둔탁하게.
  - **`chargeHum`**(start/set(p)/stop) — 단발 효과음과 달리 **누르는 동안 유지되는 유일한 소리**. 오실레이터 하나를 살려 두고 축적량으로 음정(140→420Hz)·로우패스(500→2300Hz)를 끌어올린다 = 힘이 차오르는 게 들린다. ⚠️ **반드시 회수할 것** — 놓을 때·**오버레이로 일시정지될 때**(useFrame의 정지 블록 위에서 charge를 풀어 준다)·씬 언마운트(useEffect cleanup) 세 경로 전부. 하나라도 빠지면 화면 뒤에서 영영 운다. `set()`은 음소거로 바뀌면 스스로 멎는다.
- `src/lib/music.ts` — **절차 생성 BGM** (파일 없음, 16분음표 스텝 시퀀서 + 룩어헤드 스케줄링): title(패드)/town(왈츠)/dungeon(깊이별 템포·옥타브 변화)/doorrun(질주)/boss(오스티나토 — 몬스터 아레나도 이 트랙). App useEffect가 phase·보스 생존·층 티어에 따라 트랙 전환. 음소거 토글 시 `music.sync()`. 출력은 sound.ts의 `masterBus()` 경유 — 믹스가 한 곳에서 결정된다. **적응형 강도 레이어**(2026-07-30): `music.setIntensity(0~1)`을 App이 콤보·저체력·보스HP·「마지막 문단」·역류를 합성한 값(`deriveIntensity` 순수 함수)으로 매 상태 변화마다 채워 넣으면, dungeon/boss 트랙 안에서 타악기·저음 레이어가 그 세기만큼 실시간으로 붙었다 뗀다 — **트랙 자체는 안 바뀐다**(phase 전환은 기존 그대로), 같은 곡 안의 밀도만 변한다. `tick()`이 매 스케줄 주기 intensity를 target으로 12%씩 스무딩해 급변에도 뚝뚝 끊기지 않음. DEV 검증은 `music.getIntensity()`(DungeonScene state()의 `musicIntensity`) + `deriveIntensity()` 자체가 순수 함수라 헤드리스로도 입력→출력 단언 가능.
- **보스 "페이지의 수호자"** (10층마다, DungeonScene) — 출구를 지키며 포털 봉인(처치 전 포털 숨김·비활성). 추격(2.3) + 방사 탄막 10발(30층+ 12발·60층+ 14발, 간격 `max(1.3, 2.0-층×0.012)`s) + **저체력 40% 미만 광폭화**(발사 간격 ×0.72) + 강한 접촉 피해(`16+층×0.7`). 처치 시 확정 보물 1개 + 회복 30 + 봉인 해제 연출. HUD에 보스 체력바. **hp = 500+층×50** (2026-07-18 상향 — 성장 빌드에 순삭 방지, 10층 1000). **주인공 자동 조준은 적뿐 아니라 보스도 대상**(예전엔 enemies만 봐서 일반 적을 다 치우면 보스를 못 쐈음 — 수정됨).
- **엔딩 (100층)** — 100층 출구는 황금 문. 접촉 시 `ending` phase: 혼자 나가기 / 촌장(작가)과 함께 나가기 선택 → 각각 다른 에필로그(story.ts ENDING_*) → 통계 화면. 100층에도 보스 있음(최종전 후 문).
- **기억 완성 보상** — 12번째 기억 회수 시 `memfull` phase: 아이템 2개 + 완전 회복.
- **위기 연출** — HP 30% 미만: 붉은 비네트 펄스 + 심장박동음(1초 간격).
- **인터랙티브 인트로** — STORY_NODES에 quiz 노드(책이 "7×8=?"을 물음, 정답/오답 모두 빨려 들어가는 개그). 배경 클릭 진행은 퀴즈 미답변 중 잠금.
- `src/lib/memories.ts` — **되찾는 기억 16개 + 네 갈래(2026-07-19 확장)**. 갈래: ☕사소한 날들 · 🎓교정의 계절 · 🏠돌아갈 집 · 💗말하지 못한 마음 (각 4조각). **한 갈래를 다 모으면 능력이 각성**하고 판을 넘어 남는다(메타 성장): 사소한 것들의 힘(새 층 도착 시 +6 회복) / 벼락치기(두 문 달리기 실패해도 아이템 1개) / 돌아갈 곳(판당 1회, 체력 40%로 부활) / 두근거림(체력 30% 미만 시 공격력 ×1.3). **전부(16) 완성 시** 시작 최대 체력 +30 + 기존 완성 보상(아이템 2개·완전 회복). 순수 함수 모듈(`powersOf`·`setProgress`·`newlyCompletedSet`)이라 검증이 쉽다. 회수 순서는 갈래가 번갈아 나오게 배열해 일상 → 학교 → 집 → 사랑 순으로 완성된다(감정의 정점이 마지막). 저장은 `d100-mems`(회수한 id 배열), 옛 `d100-mem`(개수)은 `migrateCount`로 자동 이어받음.
- `src/lib/story.ts` — 인트로 슬라이드·층별 벽의 글귀(getLore)·마을 대화. (기억 데이터는 memories.ts로 분리) **걸어다니는 마을 NPC 대화는 상황(TownContext: enter/visit/death)별 함수**로: `chiefTalk`(촌장 — 첫 입장 퀘스트·5층마다 세계관 회수·부활 격려), `ninaTalk`(니나 — 수프 회복, 부활 직후 '다시 쓰일 뿐'), `mukTalk`(무크 — 대장간 상점 진입), `entranceOptions`(던전 입구 — **enter일 때만** 난이도 선택. visit/death는 빈 스크립트 = 화면 없이 즉시 하강, 2026-07-27). line 노드 `next<0`이면 대화 종료. **가짜 선택 개그**(선택지는 있지만 결과는 하나): 촌장 첫 대화 "다른 방법은 없나요?"→"없어.", 니나 수프(재료를 물어도 결국 먹음), 여백 찻자리(케이크는 없음). **마을 시절 연동 대사**(villageStageOf=20층 단위, TownScene 시절과 동기화): 촌장 25층+ 방문 근황(가을→습격→방벽→폐허와 새벽), 니나·무크 첫마디 변화 — App talkTo가 villageFloor 전달(enter 허브=0). **세계관: 던전=쓰이다 만 책, 마을=서문, 촌장=작가, 100층 문=뒤표지, 여백=쓰다 만 「공주님」(이름 빈칸).** 엔딩 뒤 **10년 후 에필로그**(ENDING_EPILOGUE, 전 엔딩 공통 — 성장 회수 + 『2권에서 계속』 예고). localStorage: `d100-story`(인트로 1회), `d100-mem`(되찾은 기억 수).
- 새 층 도착 시 `lore` phase로 벽의 글귀 1개 노출, 보물 획득 후 `memory` phase로 기억 1개 복원 → 그 기억으로 **갈래가 완성되면 `memset` phase**(능력 각성 화면), 16개를 모두 채운 순간엔 `memfull`. 이미 전부 모았으면 회상 화면을 건너뛴다(완성 보상 반복 지급 방지). 5의 배수 층엔 마을 문(dungeon.ts homeDoor) — 방문은 층 유지(townMode 'visit'), 문은 1회용(homeUsedRef), 거절 시 재무장(homeRetryRef). 몬스터는 5층 단위 티어로 모양·색 변화(DungeonScene ENEMY_TIER_*).
- `src/lib/rng.ts` — mulberry32 시드 난수. **층 번호 = 시드** → 같은 층은 항상 같은 구조 (재현성).
- `src/lib/upgrades.ts` — 스탯·보상 카드 풀 **16종** (2026-07-19 확장: 치명타·폭발·탄속·관통(전설)·방어·회피·흡혈·가시·탐욕·넉백 추가) **+ 휘발되는 장 2종**(`VOLATILE_UPGRADES`, 2026-07-29 — 아래 참조). **희귀도 3단계**(common 1 / rare 0.5 / legendary 0.22 가중)와 **시너지 태그**(공격/생존/보조 — 같은 태그 2개+ 보유 시 드래프트 가중 ×1.35). 모든 지급 경로(드래프트 `draftThree(rand, build)`·보물·보스·선물·제단·기억 완성·역류·상인)가 `pickUpgrades` 가중 뽑기 공유 — 그래서 휘발 카드도 특정 지급 경로 하나가 아니라 이 함수를 거치는 모든 경로에서 나올 수 있다. **휘발되는 장**(`VOLATILE_UPGRADES` — 「찢어진 낱장」 2종: 공격력 +40 / 연사 +2.5, 둘 다 3층 뒤 자동 소멸): 픽 확률은 희귀도 대신 고정 `VOLATILE_WEIGHT`(0.15, legendary 0.22보다도 낮음)로 별도 관리(`pullWeighted`). `revert`는 `apply`의 정확한 역연산인데, **소프트 캡(dmgStep 등)을 타는 수치는 쓰지 않는다** — 고정값만 더하고 그대로 빼야 사이에 다른 카드를 몇 장 더 집어도 덧셈이 정확히 상쇄된다(소프트 캡 함수는 그 시점의 누적값에 의존해 비선형이라 나중에 빼면 어긋난다). 만료 판정은 순수 함수 `expireVolatile(expiring, stats, floorNo)` — App이 `floorNo`가 바뀔 때마다(포털·역류·찢어진 페이지 등 모든 층 전환이 결국 `floorNo`를 바꾸므로 그 지점 하나로 전부 커버) 호출해 시한 지난 카드만 stats에서 되돌리고 `build` 카운트도 함께 내린다(BuildRow 칩·시너지 판정이 사라진 효과를 계속 들고 가지 않게). 카드 UI엔 `.expire-hint`(`.evo-hint`와 같은 패턴, 호박색) 칩으로 "N층 후 소멸"을 미리 보여준다. 카드 UI에 희귀도 테두리(레어 파랑·전설 금)+태그 칩, **재료 카드엔 진화 힌트 칩**(`evoHintFor` — Evolution은 `req` 데이터로 조합 판정, 2026-07-24). 전투 반영은 DungeonScene·GemArenaScene 양쪽(관통탄은 같은 적 연속 타격 방지 `last`, 폭발 연쇄는 1단계만, 아레나 흡혈은 아레나 체력). **이동 속도는 소프트 캡**(SPEED_CAP=12, 캡까지 남은 거리의 15%씩) — 예전 곱연산 ×1.12는 픽마다 절대 증가량이 커져 고속에서 던전 조작이 불편했음(2026-07-17 변경). **사거리도 소프트 캡**(RANGE_CAP=13, 남은 거리의 15%씩) — 복리 ×1.15는 적 어그로권(9)·슈터 사거리(11)를 넘어 위험권 밖 안전 저격이 됐었음. 캡 13은 보스 교전 반경(16)·탄막 도달(15) 안쪽이라 보스를 때리려면 탄막 안에 서야 함. **공격력 ×1.25→×1.18, 연사 ×1.2→×1.14** — 층당 최대 4아이템 유입과 복리가 겹쳐 DPS가 보스 HP(선형)를 압도하던 것 완화 (2026-07-18 밸런스 패스, 검증: 15아이템 풀빌드도 보스 근접 시 2초 사망).
- `src/lib/store.ts` — useLocalStorage 훅 (함수형 업데이트 지원) + **`suspendPersistence(on)`** — 자동 시연 세이브 격리용 쓰기 차단 스위치 (위 자동 시연 항목 참조). 키는 `d100-` 접두사: `d100-best`(최고 층), `d100-story`(인트로), `d100-mem`(기억), `d100-muted`(음소거), `d100-coins`(코인), `d100-meta`(영구 강화 {dmg,hp,spd}), `d100-deaths`(사망 횟수 — 죽음 로어 진행), `d100-traces`(본 흔적 층 번호 배열 — 42 포함 여부로 소녀 대화 분기), `d100-girl`(소녀 만남), `d100-daily`(일일 던전 기록 {date,floor,cleared}), `d100-mems`(되찾은 기억 id 배열 — 옛 `d100-mem` 개수 세이브를 자동 이어받음), `d100-peddler`(떠돌이 상인 첫 만남), `d100-gfx`(그래픽 품질 pref auto/high/lite — 기본 auto), `d100-gfx-auto`(FPS 자동 판정 결과 high/lite), `d100-dex`(도감 {items,mobs,events}), `d100-dex-claim`(도감 마일스톤 수령 수), `d100-coach`(1층 조작 코치마크 1회 표시).
- **적 4타입** (DungeonScene): chaser(추격)/shooter(거리 유지+조준 사격, 2층+)/dasher(조준 후 돌진, 4층+)/tank(느리고 단단·넉백 면역·피해 1.5배, 6층+). 실루엣·색으로 구분(탱커 크고 어둡게, 슈터 작고 노랗게, 대셔 길쭉+조준 시 부들부들). 슈터 탄환은 보스 탄막 풀(eshots, 64) 공유. **난이도 스릴 램프**(2026-07-18): 추격 속도 `2.5+min(3.3, 층×0.16)`·탱커/대셔도 층 비례 가속, 슈터 연사 `max(1.2, 2.1-층×0.06)`s + 8층+ 부채 2발·20층+ 3발, 탄속 `6.5+min(3, 층×0.08)`, 스폰 방당 `min(9, 2+층/3)`(2026-07-19 캡 7→9), 위험 타입 비중 깊이 비례 상승(d=층/30). **깊은 층 위협 램프**(2026-07-19): 받는 피해 공식만 `threatFloor = 층 + max(0, 층-30)×0.6`으로 가속(접촉·탄막·보스·수문장) — 적 스탯 캡 이후에도 아이템이 복리로 쌓여 파밍 빌드가 무적 순항하던 것 제어(시뮬 실측 161아이템 61층 무저항 → 30층 보스 사망 확인). 얕은 층(≤30)·이동 감각은 불변. 검증 기준: 1층 8초 방치 HP −14(초보 안전), 7층 방치 2.8초 사망, 10층 보스방 방치 1.9초 사망.
- **출구 수문장** (elite, DungeonScene) — 보스 없는 층(4층부터)의 **포털 문 앞을 지키는 정예** 1기. dasher AI를 재사용하되 더 넓은 어그로·빠른 접근·매서운 돌진, **넉백 면역**, 크고 진홍빛(전용 광원 eliteLightRef). 돌진 강타는 HP를 크게 깎고(`16+층×0.9`, 일반 접촉 `9+층×0.5`) 붉은 데미지 숫자로 보여줘 **깊이 내려갈수록 긴장감 유지**. hp `35+층×11`, 처치 코인 12. 포털을 봉인하진 않음(하드 게이트 X — 위험만 강조). DEV state에 `guardian` 노출. **🔥 정예의 증표**(2026-07-30 — 정예를 잡으면 그 시그니처가 내 덱에 들어온다): 판당 **첫** 수문장 처치에 한해 전용 카드(`ELITE_SIGNATURE`, upgrades.ts — 공격력 +16·방어 +12%)를 자동 지급, `.ghost-banner` 토스트로만 알림(화면 전환 없음, rushWin과 같은 패턴). 일반 드래프트·보물 풀(`pickUpgrades`)엔 안 섞인다. **판당 1회로 못박은 이유**: 수문장은 4층부터 거의 매 층에 서 있어 처치마다 지급하면 아직 미해결인 "성장은 복리, 위협은 선형" 스노볼 과제(로드맵 참조)를 키운다 — 런당 아이템 +1로 영향을 한정. ⚠️ **지급은 처치 순간이 아니라 다음 `onExit`(포털 통과)로 미룬다** — App의 onKill은 ref 플래그만 세운다. 실측: 처치 즉시 `gainUpgrade`(스탯+빌드+도감 setState 묶음)를 불렀더니 하드런 사망 중앙값이 8→4로 무너졌다. 수문장은 다른 적과 뒤엉킨 채 죽는 경우가 많아, 그 프레임에 무거운 리렌더가 겹치면 실전투 중 프레임이 튀어 접촉 피해 판정이 불안정해진 것 — 수문장은 포털 바로 앞을 지키므로 처치 직후 곧 포털로 들어가고, 그 순간은 이미 층 전환이 시작돼 실전투가 아니다(「무너지는 서가」 보상과 같은 안전 지점 재사용). **🧊 저항하는 정예**(2026-07-31 — 찰나에 카운터플레이를 준다): 수문장은 **찰나(시간 동결)에 저항**한다 — 얼려진 블록 안에서 다른 적은 `dt=0`을 받지만 수문장만 `e.elite ? worldDt : frozenDt`로 실제 시간 그대로 움직이고 공격 판정(`!frozenNow || e.elite`)도 살아 있다. 얼어붙은 채로 수문장에게 맞으면 남은 찰나 지속시간을 `FREEZE_STEAL`(0.35초)만큼 도둑맞는다(청록 파티클로 신호) — "정예 앞에서 얼려도 안전하지 않다"는 실시간 판단을 만든다. 찰나 발동 순간 수문장이 살아 있으면 그 자리에 붉은 균열 이펙트로 미리 경고한다. 결의(무적 대시) 중엔 `hurtPlayer`가 애초에 0을 반환해 이 상호작용을 안 탄다. 보스·일반 탄막·다른 적 4종은 기존대로 완전히 언다(대상은 수문장 하나뿐).
- **메타 성장**: 처치 코인(일반 1, 탱커 3, 보스 25) → 마을 대장간(무크, phase `shop`)에서 영구 강화 구매 — 공격/생명/신속 단련 **레벨 무한**(2026-07-17, 예전 5렙 상한 제거), 비용 (lv+1)×25. 공격 +2/생명 +15는 선형, **신속은 소프트 캡**(metaSpeed: 레벨당 SPEED_CAP=12까지 남은 거리의 8%씩 — 1레벨 +0.4로 예전과 동일, 무한 구매해도 캡 안쪽). enterDungeon에서 시작 스탯에 반영. **코인·강화는 죽어도 유지.**
- **죽음 로어**: 사망할수록 "다시 쓰이고 있다"는 위화감이 커지는 문구(getDeathLore, 5단계+순환) — 게임오버 화면에 표시, 죽음=페이지가 넘어가는 것이라는 세계관과 연결.
- **부활 체크포인트** (App `checkpointFloor`): 5층 단위 마을 문에 들를 때마다(openHomeDoor) 그 층으로 갱신. 죽으면 1층이 아니라 **마지막 다녀온 마을에서 부활** — 게임오버 화면 "🏘️ N층 마을에서 다시 (장비 유지)" → 걸어다니는 마을(`goVillage('death')`)에서 니나·촌장이 맞아 줌 → 던전 입구로 걸어가 그 층부터 재개(**빌드·스탯 유지·완전 회복**, runId 증가로 씬 리마운트). 체크포인트<5(마을 미방문)면 기존처럼 "바로 다시 도전"(1층). enterDungeon에서 1로 초기화. '주인공은 다시 쓰일 뿐'이라는 죽음 로어 세계관과 직접 연결.
- **56층의 소녀 '여백'**: 작가가 쓰다 만 이름 없는 등장인물 (17층 글귀 회수). **복선 흔적**이 14(낙서)·28(종이학)·42(초대장)·49(찻잔)층에 오브젝트로 배치(TRACE_FLOORS, 접촉 시 `trace` phase 오버레이). 흔적을 보면 `d100-traces`에 층 번호 기록. 56층(GIRL_FLOOR)에서 만나면 대화(**girlScript(sawInvite)** — 42층 초대장을 봤으면 첫인사가 달라지고 **답례 아이템 1개 추가**, townMode 'girl'·전용 배경) — 아이템+차(완전 회복) 선물, 작가에게 전할 부탁(공주 이름 포함). 만난 기록은 `d100-girl`, **엔딩에 장면 추가**(ENDING_GIRL_EXTRA — 전 엔딩, ENDING_NAME_PRINCESS — 함께 엔딩 한정 '공주의 이름'). 소녀·흔적 주변은 몬스터 스폰 제외(28층 복선과 일치). 방문 선물은 노드당 1회(visitGiftGiven Set).
- `index.html` — `?rafshim` 심: 타이머 구동 rAF + ResizeObserver 폴링 폴리필 (숨김 탭 검증용 — 없으면 r3f가 부팅 못 함).

## 원칙
- UI 문구는 한국어. 백엔드 없음 — localStorage만.
- **절차 생성이 기본값 — 외부 리소스는 라이선스가 허용할 때만, 원본 파일 반입은 최대한 자제** (2026-07-27 규칙 완화). 예전 규칙은 '외부 에셋 전면 금지'였으나, 이제 라이선스가 명확한 외부 리소스는 쓸 수 있다. 단 **3단 사다리**를 위에서부터 시도한다: ①코드로 생성(지오메트리·캔버스 텍스처·Web Audio — 그래픽은 `src/three/fx.tsx`, 사운드는 `src/lib/sound.ts`가 그 출발점이고 파일 머리에 이 규칙이 적혀 있다) → ②원본 파일 없이 참조/규격만(CDN 웹폰트처럼) → ③원본 파일(png·mp3·glb) 반입은 최후 수단. 복잡성이 파일이 아니라 코드에 있어야 AI가 읽고 고치고 자동 검증할 수 있다는 이유는 그대로다 — 바이너리가 늘수록 봇·스모크가 손댈 수 있는 범위가 줄어든다.
  - **종류별 정책**: 폰트 = 허용(OFL/CC0 + **시스템 폰트 폴백 필수**) / 그래픽 = 허용하되 스타일 통일이 라이선스보다 먼저 / 사운드 = **BGM은 합성 유지**, 발소리·문 같은 단발 실물 소리만 반입 가능(합성이 약한 자리가 거기다).
  - **원장은 기계가 읽는 파일, 문서는 그 생성물**: 원본은 `docs/assets.json`이고 `docs/ASSETS.md`의 표는 `npm run assets-write`로 생성된다 — 손으로 쓴 표는 반드시 낡기 때문. 라이선스 게이트 6항목(1차 출처·이름 있는 라이선스·상업/재배포 허용·귀속 이행 가능·개작 허용·**NC/ND 아님**)을 통과시킨 뒤 JSON만 고친다. `files` 항목은 `why`(절차 생성으로 대체 못 하는 이유)가 비면 검사가 실패한다.
  - **규칙은 코드가 지킨다**: `npm run assets-check`(`scripts/assets-check.mjs`)가 ①런타임 트리(`src`/`public`/`index.html`)의 바이너리 리소스 ②코드가 실제로 로드하는 외부 호스트(`<link href>`·`<script src>`·`url()`·`fetch()` — 하이퍼링크·XML 네임스페이스는 제외) ③직접 의존성 라이선스(package.json 실측, MIT/Apache/BSD/ISC/CC0/OFL 계열만) ④ASSETS.md 표가 원장과 일치하는지를 대조해 어긋나면 exit 1. **deploy-pages.yml의 typecheck 다음에 편입**돼 대장에 없는 것이 섞여 들면 배포가 멈춘다. 위반 주입 테스트로 실제 실패하는 것까지 확인했다(통과만 하는 게이트는 죽은 장치).
  - **반입한 것은 없어도 서게 한다**: 웹폰트는 `main.tsx`가 부팅 때 `document.fonts.load`로 명시적으로 받고(안 받으면 캔버스에 구운 텍스처에 **폴백 서체가 박혀** 영영 안 바뀐다 — 문제판·이름표가 그렇다) **1.2초 상한** 뒤엔 그냥 진행한다. 실측: 폰트 CDN을 막아도 타이틀까지 459ms.
- **외부 저작물을 저장소에 들이지 않는다** (2026-08-01 강화). 이 저장소에 들어가는 것은 **직접 쓴 코드와 절차 생성물뿐**이다. 다음은 예외 없이 금지:
  - **남의 텍스트를 옮겨 적지 않는다** — 기사·리뷰·문서의 문장을 인용·번역·요약 형태로도 코드 주석이나 문서에 넣지 않는다. 설계 근거는 **이 게임 안에서 성립하는 말로만** 쓴다("예전엔 100% 안전한 버튼이라 판단이 필요 없었다"처럼).
  - **남의 코드·에셋·사운드·아트를 들이지 않는다** — 위 3단 사다리와 `assets.json` 원장·`npm run assets-check`가 이미 배포 게이트에서 막는다.
  - **다른 작품의 고유명사를 쓰지 않는다** — 작품명·캐릭터명·고유 아이템명·세계관 용어. 게임 화면 문자열은 물론 **코드 주석·설계 문서·커밋 메시지에도** 쓰지 않는다.
  - 설계 논의에서 다른 게임이 화제에 오르는 것 자체는 막을 수 없지만, **저장소에 남기는 것은 이 게임의 규칙과 그 근거뿐**이다. 장르 관습(로그라이크의 층·드래프트, 셀룰러 오토마타 같은 공개 알고리즘)은 특정 작품에 귀속되지 않으므로 이 규칙과 무관하다.
- 게임 로직은 렌더와 분리된 순수 함수 지향 (밸런스 자동 시뮬레이터가 로드맵에 있음 — 헤드리스로 돌릴 수 있어야 함).
- 커밋 기록이 곧 개발 과정의 증빙 — 작업 단위마다 의미 있는 한국어 커밋 메시지로 커밋하고, **작업이 끝나면 항상 바로 push** (main 푸시 = Pages 자동 배포, 2026-07-17 지시).

## 완료 노트 (최근 순)

> 무엇을 왜 그렇게 만들었는지의 기록. **새 작업을 시작하기 전에 관련 날짜를 훑어** 같은 함정을
> 다시 밟지 않게 한다 (특히 ⚠️ 표시가 붙은 줄 — 전부 실제로 한 번 당한 것들이다).

### 2026-07-31 — 저항하는 정예 (찰나 카운터플레이)

07-27에 넣은 찰나(시간 동결)는 지금까지 **눌리면 100% 안전한 버튼**이었다 — 얼면 적·보스·탄막이
전부 멈추니 판단이 필요 없었다. 자원을 쥐고도 "지금 써도 되나"를 묻게 만들려면 얼음이 안 통하는
예외가 하나는 있어야 한다고 보고, **출구 수문장(정예)만** 찰나에 저항하게 했다.

- **왜 수문장인가**: 새 확률부 적 타입을 추가하는 대신, 이미 "정예"라 불리며 층마다 최대
  1기뿐인 기존 개체(`e.elite`)를 재사용했다. 등장 빈도가 이미 조절돼 있고(4층부터, 보스층
  제외), 시각적으로도 이미 남달라(크고 진홍빛·전용 광원) "이 녀석은 다르다"는 인상이 있어
  새 규칙을 얹기 좋은 자리였다. **정예의 증표**(07-30)와 같은 개체를 다른 각도(보상 → 위험)
  에서 한 번 더 의미 있게 만든 셈이다.
- **구현 — dt 셰도잉의 범위를 좁혔다.** 찰나는 `{ const dt = frozenNow ? 0 : worldDt; ... }`
  블록으로 적 AI·보스·탄막 전체를 한 번에 가리는 방식이었다(07-27). 이 블록 이름을
  `frozenDt`로 바꾸고, **적 루프 안에서만** `const dt = e.elite ? worldDt : frozenDt;`로
  다시 그림자를 씌웠다 — 그 한 줄 아래의 walk()·hitCd·모드 타이머는 전부 기존 `dt`를 그대로
  참조하므로 로직을 한 곳도 더 안 건드리고 수문장만 실제 시간으로 움직인다. 보스·탄막은
  블록 최상위 `frozenDt`를 직접 쓰도록 이름만 바꿔 기존 동작(완전히 언다) 그대로 유지했다.
  접촉 피해 게이트도 `!frozenNow` → `!frozenNow || e.elite`로 한 곳만 넓혔다.
- **"타이머를 훔친다" = `FREEZE_STEAL`(0.35초).** 얼어붙은 채로 수문장에게 맞으면
  `freezeActiveRef.current`를 그만큼 깎는다 — 얼음 상태에서 실제로 손해를 보는 유일한 경로라
  "정예 앞에서 지금 얼려도 되나"가 진짜 판단이 된다. 결의(무적 대시) 중엔 `hurtPlayer`가
  애초에 0을 반환해(기존의 단일 가드) 이 상호작용이 안 탄다 — 별도 예외 처리를 추가하지 않아도
  기존 가드가 알아서 막아 줬다.
- **경고 신호**: 찰나를 발동하는 순간 살아 있는 수문장이 있으면 그 자리에 붉은 균열
  (`ripple`+`burst`)을 한 번 터뜨린다 — 맞고 나서 "왜 안 얼었지?"로 깨닫기보다, 쓰기 전에
  "저건 안 통한다"를 보여 주는 쪽을 택했다(07-27 붉은 포털과 같은 원칙: 화면 텍스트 대신
  씬 안의 신호로 답한다).
- **검증**: `npm run typecheck`·`npm run build`·`npm run floor-check`(300층, 지형은 안
  건드렸지만 회귀 확인차) 전부 통과. `npm run assets-check` 통과. **`RUNS=3
  balance-regression`(하드런) — 사망 6·6·9(중앙값 6, 기준선 5~13 안)·stuck 0** — 하드런은
  찰나를 아예 안 눌러서(탭 0) 이 변경과 완전히 무관함을 다시 확인한 셈이다.
  - ⚠️ **미검증 — 라이브 상호작용.** 찰나·결의(07-27/07-29)와 같은 전례를 그대로 따른다:
    수문장 근처에서 찰나를 실제로 발동시켜 저항·도둑맞기가 눈으로 보이는지는 사람형 봇의
    의도적 판단 로직이 아직 없다(human 프로필은 위험/포위 여부만 보고 찰나를 쓸 뿐, "여기
    수문장이 있으니 쓰지 말자"는 아직 계산하지 않는다). 다음 세션에서 simBot human 프로필의
    `decideRoomEvent`류 판단부에 "찰나 발동 전 반경 안에 살아 있는 정예가 있으면 보류"를
    추가하고, `FREEZE_STEAL`이 실제로 깎이는지까지 재는 것을 남겨 둔다(찰나·결의가 07-27에
    미검증으로 남았다가 07-29에 실측된 것과 같은 순서).
  - **추가 실측(같은 날, `PROFILE=human MAXFLOOR=10 RUNS=2`)**: 2판 전부 무사(사망 없음·
    stuck 0), 찰나 발동 6회·결의 발동 16회 모두 **발동 중 피해 0**(결의는 원래 항상 0이어야
    하는 불변식, 찰나도 이번엔 0). 다만 이건 "저항이 안전하다"는 증거가 아니라 **이 표본에서
    찰나 발동 순간에 수문장이 접촉 범위 안에 없었다**는 뜻일 뿐이다 — human 프로필이 아직
    수문장 근접 여부를 안 보고 찰나를 쓰므로, 우연히 안 겹친 것과 "저항·도둑맞기가 실제로
    작동한다"를 이 데이터만으로는 못 가른다. 위 미검증 항목은 그대로 유효.

### 2026-07-30 — 적응형 강도 사운드트랙 · 정예의 증표

**적응형 강도 사운드트랙**(`music.ts` `setIntensity`/`deriveIntensity` — 콤보·저체력·보스HP·
「마지막 문단」·역류를 0~1로 합성해 dungeon/boss 트랙 안에서 타악기·저음 레이어가 실시간으로
붙었다 뗀다)과 **정예의 증표**(`upgrades.ts` `ELITE_SIGNATURE` — 판당 첫 출구 수문장 처치에
전용 카드 자동 지급)는 위 「구조」 절의 `music.ts`·「출구 수문장」 항목에 상세를 적어 뒀다.

- ⚠️ **정예의 증표가 처음엔 하드런 사망 중앙값을 8→4로 무너뜨렸다 — 실전투 중 무거운
  setState 배치가 프레임을 튀게 만든 것.** 처음 구현은 수문장을 처치한 그 프레임에
  `gainUpgrade`(스탯+빌드+도감 갱신 setState 3~4개)를 곧바로 불렀다. `RUNS=6
  balance-regression`을 돌렸더니 사망 층이 4,4,4,4,5,6으로 뭉쳐 나왔다 — 정확히 수문장이
  등장하기 시작하는 층. **처음엔 이 컨테이너의 SwiftShader 헤드리스 환경이 원래 불안정한가
  의심했다** — 그래서 `git stash`로 변경분을 뺀 순정 main을 똑같은 설정(`PW_EXECUTABLE`)으로
  대조군 실행해 사망 층 5,5,6,8,9,9(중앙값 8, 정상)를 확인했다. 환경 문제가 아니라 내
  변경이 원인이라는 뜻이었다.
  - **이분탐색으로 원인을 좁혔다**: ①정예 보상 트리거를 완전히 꺼 보니(적응형 사운드트랙은
    그대로 둔 채) 8,8,9로 정상 — 사운드트랙 쪽은 무죄. ②`setEliteMark`(배너 상태)만 끄고
    `gainUpgrade`는 남겨 두니 6,6,6(기준 안이지만 여전히 낮음, n=3이라 불확실) — 배너
    렌더보다는 `gainUpgrade` 자체(또는 그 조합)가 주범에 가까웠다.
  - **결론**: 수문장은 포털 바로 앞을 지키는 정예라 다른 적들과 뒤엉켜 처치되는 경우가
    많다 — 그 순간에 무거운 리렌더(스탯 객체 통째 교체 + 빌드 맵 갱신 + 도감 Set 갱신)가
    겹치면, 헤드리스 SwiftShader처럼 렌더 예산이 빠듯한 환경에서 프레임이 튀어(`?rafshim`은
    벽시계 기준 dt라 실제로 델타가 커진다) 접촉 피해 판정이 그 튄 프레임에 누적된 것으로
    보인다. 텔레포트 금지 원칙과 같은 계열의 교훈 — **"사람이 못 만드는 상황"** 대신 이번엔
    **"게임이 스스로 만드는 순간적 렌더 스파이크"**가 전투를 불공평하게 만들었다.
  - **고친 방법**: `onKill`에서는 ref 플래그(`eliteMarkPendingRef`)만 세워 두고(리렌더 없음),
    실제 `gainUpgrade` 호출은 **`onExit`(포털 통과 시점)로 미뤘다** — 「무너지는 서가」 보상이
    이미 같은 지점에서 안전하게 지급되고 있던 것과 동일한 패턴. 수문장을 잡으면 어차피 그
    문으로 들어가게 되므로 체감 지연은 거의 없다.
  - **재검증**: `RUNS=6 balance-regression` → 사망 6,6,6,8,9,9(중앙값 8) — 대조군과 사실상
    동일한 분포로 복귀, stuck 0.
  - **교훈**: **실시간 전투 도중 무거운 React 상태 갱신(특히 여러 setState를 한 콜백에 몰아
    부르는 것)은 헤드리스 자동 검증에서 실제 게임플레이 저하로 드러날 수 있다.** 이 프로젝트의
    모든 기존 보상 지급 지점(보물상자·보스·마을 선물·역류)은 전부 실전투가 잠시 멎는 순간
    (미니게임 결과·포털 통과·대화창)에서만 지급되고 있었다는 걸 이번에 새로 깨달았다 — 다음에
    "처치 즉시 보상" 같은 패턴을 또 만들 땐 이 규칙을 먼저 떠올릴 것.
- **검증**: `npm run typecheck`·`npm run build`·`npm run floor-check`(300층)·`npm run
  assets-check` 전부 통과. `npm run smoke`는 기존과 동일한 사전 존재 Google Fonts CDN
  차단 1건만 실패(이번 변경과 무관, 순정 main에서도 동일 재현 확인). `RUNS=6
  balance-regression`(수정 후) 통과.
- ⚠️ **미검증**: 사람형 프로필(`PROFILE=human`)로 정예의 증표 픽률·체감, 적응형 사운드트랙의
  실제 청감(헤드리스로는 오실레이터 개수만 셀 뿐 "잘 들리는지"는 사람이 들어야 안다).

### 2026-07-29 — simBot이 발밑 두 버튼을 실제로 누른다 — 찰나·결의 라이브 검증

07-27(찰나)·07-29(결의)이 나란히 "버튼 탭 → 실제 발동"을 미검증으로 남겼던 것을 메웠다. 봇이 게이지를 채우고 **진짜 DOM 버튼에 pointerdown을 쏘아** 발동시킨다.

- ⚠️ **왜 지금까지 안 잡혔는가 — `btn.click()`으로는 영원히 안 눌린다.** 두 훅(`useFreezeInput`/`useResolveInput`)이 듣는 이벤트는 `click`이 아니라 **`pointerdown`**이다. 봇의 기존 만능 도구인 `clickBtn()`은 `.click()`을 호출하므로, 설령 셀렉터를 알았어도 아무 일도 안 일어났을 것이다. 그래서 `tapAbility()`가 `new PointerEvent('pointerdown')`를 직접 쏘고, 버튼을 못 찾을 때만 키(F/R)로 대체한다 — **어느 경로로 발동했는지를 리포트에 남겨** "버튼이 실제로 먹혔다"와 "키로 우회했다"를 구분한다.
- ⚠️ **아레나에서는 `.resolve-btn`이 둘이다.** 아레나 중에도 DungeonScene은 마운트된 채(hidden)라 자기 몫의 💨 버튼을 들고 있다. 사람 눈에는 정지된 쪽이 훅의 150ms 폴링에 가려져 하나만 보이지만, **헤드리스에서는 그 타이머가 스로틀에 죽어 display가 낡은 채 남아** DOM만으로는 '보이는 버튼'을 못 가린다. `querySelector` 하나만 쓰면 정지된 던전 버튼을 눌러 놓고 "탭했다"고 세는 유령 집계가 난다. 그래서 **전부에 쏜다** — 정지된 씬의 버튼은 훅 안에서 `pausedRef`로 스스로 무시하므로 오발동이 없다.
- **판단 기준은 사람이 왜 누르는지 그대로**: ❄️ 찰나(공격 자원) = 반경 4.5 안 적 3기+ · 보스 7 이내 · 다가오는 탄 2발+ / 💨 결의(회피 자원) = 가장 가까운 적 1.8 이내(저체력이면 3.2) 또는 역류·「마지막 문단」 중 이동 시. 대시 방향은 그 순간의 입력이라, 위험할 땐 기존 회피 벡터(적 반대쪽)로, 시간에 쫓길 땐 출구 쪽으로 자연히 튄다 — **방향을 위해 따로 로직을 만들지 않았다.**
- **`hpLossWhileActive`가 이번 측정의 핵심 단언이다.** 탭 수·발동 수만 세면 "눌리긴 했다"까지밖에 못 간다. 발동 순간의 체력을 기억해 두고 꺼질 때 비교해, **무적인 동안 체력이 한 번이라도 깎였는지**를 센다 = `hurtPlayer` 단일 가드가 실제 전투에서 샜는지를 재는 회귀 장치.
- ⚠️ **hard 프로필은 한 번도 안 누른다** — 모든 발동은 `human()` 게이트 안에 있다(`balance-baseline.json`이 hard로 잡혀 있어 동작을 바꾸면 야간 회귀가 통째로 무의미해진다). 상태 추적(`trackAbility`)만 프로필 무관하게 도는데, 이건 읽기 전용이라 시뮬레이션에 영향이 없다.
- **실측**: ①`PROFILE=human MAXFLOOR=8 RUNS=2` — 찰나 충전 6·탭 4·**발동 4**, 결의 충전 11·탭 8·**발동 8**, 전부 버튼 경로, 발동 중 피해 0 ②`PROFILE=human MODE=monster` 같은 조건 — 찰나 6발동·결의 **19발동**(아레나에서 무리를 뚫는 일이 잦다), 전부 버튼 경로·피해 0, 아레나 16/16 ③**하드런 3판 회귀 통과** — 사망 8·8·9(중앙값 8, 기준선 5~13 안)·stuck 0, **탭 0**(게이지가 차도 안 누른다는 게 수치로 확인). typecheck 통과.
- **딸려 나온 관측**: 아레나 클리어율이 결의까지 쓰는 조건에서도 **16전 16승 100%** — 후보 3번(아레나 난이도 상향)의 근거가 하나 더 쌓였다.

### 2026-07-29 — 저주 단계 「믿을 수 없는 화면」 — 스케치만 남기고 코드는 안 씀

광기 게이지 같은 숫자 자원 대신 **화면이 가끔 거짓을 보여주는** 조용한 지각 왜곡으로 CLAUDE.md 백로그 6번 "저주 단계·무한 모드"를 정교화하려던 항목. **의도적으로 코드 구현을 하지 않고 설계 스케치만 남겼다** — 이유와 스케치 전문은 `docs/DESIGN.md`의 "저주 단계 — 「믿을 수 없는 화면」 설계 스케치" 절.

- **판단 근거**: 저주 단계·무한 모드 자체가 이 저장소에 전혀 없는 상태에서 지각 왜곡만 먼저 심으면, 그게 왜 켜져 있는지 설명할 상위 맥락(무슨 조건의 회차인가)이 없다. 아무 기존 층에나 끼워 넣으면 일반 플레이를 갑자기 헷갈리게 만드는 하향 조정이거나, 저주라는 개념과 무관하게 뜬 장식이 된다 — 둘 다 "장식이 아니라 실제 의사결정을 늘리는가"라는 이 프로젝트의 채택 기준을 통과하지 못한다. 반대로 저주 단계 시스템 전체(회차 수정자 체계)를 이번 세션에 새로 설계·구현하는 건 로드맵 6·7번급 별도 작업이라 무리라고 판단했다.
- **지시받은 축소안**(최소 시험 단위: 100층 클리어 후 보너스 스테이지 1개, 또는 기존 room event에 임시 적용)도 검토했지만, 후자는 "저주"라는 정체성이 안 서고("무너지는 서가가 미니맵도 흔든다"가 될 뿐), 전자는 새 phase·새 진입/종료 흐름이 필요해 이번 세션 나머지 예산 안에서 결의·휘발되는 장만큼 검증까지 마치기 어렵다고 봤다. 미완성 절반 구현(예: 보너스 스테이지 뼈대만 만들고 지각 왜곡은 안 붙임, 또는 지각 왜곡은 붙였는데 라이브 검증을 하나도 못 함)보다 스케치가 낫다는 원칙을 그대로 따랐다.
- **남긴 것**: ①왜 이번에 안 만들었는지 ②지켜야 할 안전 제약(HUD 표시값만 왜곡, DEV 훅이 돌려주는 진짜 좌표·충돌·시뮬레이션 값은 절대 불변 — 안 지키면 floor-check·simBot의 '진실'이 오염된다) ③최소 시험 단위 후보 3개(규모순) ④다음 세션이 바로 쓸 수 있는 순수 함수 스케치(`illusionOffset(seed, t)` — 던전 생성 rand와 분리된 별도 시드, 초 단위 느린 주기로 갱신해 카메라 셰이크 때와 같은 멀미 문제를 피함) ⑤검증 방법(DEV 훅 좌표가 왜곡 on/off와 무관하게 항상 동일하다는 단언 하나로 충분, 헤드리스 자동화 가능).
- **커밋**: 코드 변경 없이 문서(CLAUDE.md·docs/DESIGN.md)만 갱신하는 커밋 하나만 만든다.

### 2026-07-29 — 휘발되는 장 — 즉시 강함과 잃을 것의 트레이드오프

"즉시 강하지만 곧 사라지는 자원"을 드래프트 카드로 옮겼다. 「찢어진 낱장」 2종(공격력 +40 / 연사 +2.5, 둘 다 기본값의 몇 배에 달하는 큰 수치)이 아주 낮은 확률(`VOLATILE_WEIGHT=0.15`, legendary 0.22보다도 낮게)로 드래프트에 섞여 나오고, 뽑으면 즉시 전투가 확 달라지지만 **3층 뒤 자동으로 사라진다**.

- **핵심 설계 결정 — "정확히 되돌릴 수 있는 것만 시한부로 만든다."** `stats`는 픽마다 `apply(s)`를 누적 적용하는 방식이라(개별 픽의 기여도를 따로 추적하지 않음) 나중에 "이 카드만 빼기"가 일반적으로 불가능하다. 공격력·연사 카드처럼 소프트 캡(`dmgStep`/`rateStep`)을 타는 수치는 그 시점 누적값에 비선형으로 의존해서, 나중에 그냥 빼면 사이에 다른 카드를 몇 장 더 집었는지에 따라 값이 어긋난다. 그래서 휘발 카드는 **소프트 캡과 무관한 고정 수치만** 쓴다(damage/fireRate에 상수를 더하고, 만료 시 같은 상수를 그대로 뺀다) — 덧셈·뺄셈은 결합·교환 법칙이 성립해 사이에 무엇을 더 집어도 항상 정확히 상쇄된다. `revert`를 `apply`의 짝으로 데이터에 직접 박아 뒀다(`{ apply, revert }` 쌍).
- **지급 경로는 하나가 아니라 `pickUpgrades`를 쓰는 전부다** — 드래프트뿐 아니라 보물상자·보스·제단·마을 선물·기억 완성·역류(무너지는 서가)·떠돌이 상인까지 전부 같은 함수를 공유하는 게 이 프로젝트의 기존 원칙이라, 휘발 카드도 그 모든 경로에서 나올 수 있다. 그래서 "언제 카드를 얻었는지"를 기록하는 `trackExpiring` 훅도 App의 지급 경로마다(`gainUpgrade`·`grantRewards`·`onBossDown`·기억 완성 인라인 2곳) 전부 심어야 했다 — 실제로 인라인으로 직접 `setStats`하는 기억 완성 보상 2곳은 처음엔 빠뜨릴 뻔했다(공유 헬퍼를 안 쓰고 그 자리에서 `picks.reduce((s,u)=>u.apply(s),stats)`를 직접 하고 있었다).
- **만료는 "층 전환 이벤트"가 아니라 `floorNo` 자체를 감시**하는 `useEffect([floorNo])`로 처리했다 — 포털·역류 성공·찢어진 페이지(2개 층 건너뛰기)·체크포인트 부활까지 층을 바꾸는 경로가 여럿이라 각각에 훅을 심는 대신, 결국 전부 `floorNo`를 바꾼다는 공통점 하나로 묶었다. 순수 함수 `expireVolatile(expiring, stats, floorNo)`가 시한 지난 항목만 골라 `stats`를 되돌리고, App은 같은 개수만큼 `build` 카운트도 내린다(안 내리면 BuildRow 칩이 사라진 효과를 계속 보여주고, 시너지 태그 판정도 유령 카운트를 반영하게 된다).
- **UI**: 기존 진화 힌트 칩(`.evo-hint`) 패턴을 그대로 재사용한 `.expire-hint`(호박색, 다른 두 칩과 색으로 구분 — 보라=합본 진행도·금빛=합본 완성·호박=곧 소멸)로 "N층 후 소멸"을 드래프트 화면에서 미리 보여준다. 희귀도 테두리·태그 칩은 손대지 않고 그대로 재사용(legendary로 지정해 시각적 무게감을 맞췄다).
- ⚠️ **의도적으로 남긴 단순화**: 시너지 태그(`synergyTags`)는 `UPGRADES`만 훑고 휘발 카드는 애초에 집계하지 않는다(태그 시너지가 몇 층 뒤 사라질 카드까지 밀어주면 드래프트가 과하게 흔들린다고 판단). 반대로 `build` 카운트 자체는 만료 시 정확히 내리므로, 도감(`dexAdd`) 기록만 "한 번 봤다"는 사실로 영구히 남고 현재 효과는 정확히 사라진다 — 이 둘의 구분(발견 기록 vs 현재 효과)이 이 카드의 정체성과 맞는다고 봤다.
- **검증**: `npm run typecheck`·`npm run build`·`npm run floor-check`(300층, 지형 생성기 무관하지만 회귀 확인차)·`npm run assets-check` 통과. `npm run smoke`는 1순위 항목과 같은 사전 존재 Google Fonts CDN 차단 1건만 실패(이 변경과 무관). **`RUNS=6 npm run balance-regression`(하드런)**: 사망 층 6·6·6·6·7·9(중앙값 6, 기준선 5~13 안) · stuck 0 · 「마지막 문단」 2회 겪고 사망 0 — **픽률이 낮게 잡혀 있어 하드런 6판에서 뽑힌 흔적이 안 보였고, 그래서 회귀도 없었다**(하드런은 상자도 안 열고 이벤트도 다 거절하는 최소 플레이라 드래프트 자체를 적게 거친다 — 표본이 적어 "안 뽑혔다"와 "영향 없다"를 완전히 구분하지 못한 채로 넘긴 한계가 있다).
  - ⚠️ **미검증**: 사람형 프로필(`PROFILE=human`)로 픽률·실제 체감(대폭 강해졌다가 훅 사라지는 순간의 위화감)을 실측하지 않았다. 하드런 표본만으로는 "낮은 확률로 뽑혀도 그 판의 나머지 진행에 문제가 없다"까지만 말할 수 있고, "이 카드가 실제로 재미있는 결정을 만드는가"는 사람형 실측이나 실기기 플레이가 답해야 한다.

### 2026-07-29 — 결의(무적 대시) — 회피를 실시간 판단으로

백층 던전은 회피가 드래프트 패시브 확률(캡 45%, `stats.dodge`)뿐이라 **실시간 회피 판단이 아예 없었다** — 그 공백을 처치 게이지 자원으로 메웠다.

- **결의 게이지**(`DungeonScene.tsx`/`GemArenaScene.tsx`) — 처치 4회(찰나의 6회보다 짧게: 방어 자원이라 더 자주 쥐어야 한다)마다 차는 게이지, 가득 차면 발밑 💨 버튼(`useResolveInput`, R키 대응)으로 0.4초 무적 대시가 발동한다. 발동 순간의 입력 방향(없으면 바라보는 방향)으로 돌진 방향을 고정하고, 그동안 자동 발사도 멈춘다(차지 샷 중 `fireTimer` 억제와 같은 패턴 재사용).
- **찰나와 대칭이지만 완전히 분리** — 같은 처치 이벤트가 두 게이지를 동시에 독립적으로 채우고(ref·훅·버튼·키 전부 별개, F=찰나/R=결의), 방향은 정반대다(찰나=세계를 멈춰 여유를 삼, 결의=짧게 무적으로 뚫고 지나감). 혼동 방지를 위해 아이콘(❄️/💨)·색(청록/금빛)·버튼 위치(엄지 자리 스택의 2번째/3번째 칸)도 다르게 뒀다.
- ⚠️ **무적 가드는 찰나처럼 호출부마다 걸지 않고 `hurtPlayer` 함수 안 한 곳에만 뒀다.** 지시받은 방식은 찰나의 `!frozenNow`처럼 접촉 판정 3곳에 가드를 추가하는 것이었지만, 실제로 코드를 훑어보니 이 게임의 플레이어 피해 판정은 이미 던전 4곳(적 접촉·수문장 돌진·탄막·좀먹은 바닥)·아레나 2곳이 전부 `hurtPlayer`라는 단일 함수를 거치고 있었다(기존 `dodge` 스탯도 그 함수 안에서 처리). 호출부마다 가드를 흩뿌리면 세는 것부터 틀리기 쉽고(실제로 "3곳"이라던 접촉 판정이 세어 보니 4곳이었다) 새 피해 판정이 추가될 때 빠뜨릴 위험이 있다 — 그래서 단일 함수 안에 한 줄(`if (resolveActiveRef.current > 0) return 0`)로 넣는 쪽이 더 안전하다고 판단해 지시된 패턴 대신 이 방식을 썼다. 던전·아레나 양쪽 다 `hurtPlayer`가 이미 있어 그대로 적용된다.
- **GemArenaScene은 찰나가 아예 없었다**(이번에 처음으로 결의만 이식) — `RESOLVE_KILLS`/`RESOLVE_DASH_DURATION`/`RESOLVE_DASH_SPEED`/`useResolveInput`을 DungeonScene에서 export해 `useMoveInput` 재사용과 같은 패턴으로 아레나가 그대로 가져다 썼다. 킬 퍼널(`killEnemy`)도 양쪽 다 단일 함수라 게이지 증가 로직을 한 곳씩만 추가하면 됐다.
- 효과음 2종(`resolveReady`/`resolve`) 추가 — 찰나의 서늘한 사인파와 대비되는 따뜻한 삼각파+노이즈 스윕으로 "같은 자원이 아니다"를 귀로도 구분되게 했다. sound.ts 레시피 수 표기가 CLAUDE.md에서 이미 24종으로 낡아 있던 것(실제로는 07-27에 찰나 2종이 추가돼 26종이었다)도 이번에 28종으로 같이 바로잡았다.
- **검증**: `npm run typecheck`·`npm run build`·`npm run floor-check`(300층, 지형 생성기는 안 건드렸지만 회귀 확인차 실행)·`npm run assets-check` 전부 통과. `npm run smoke`는 콘솔 에러 1건(Google Fonts CDN 차단)만 실패했는데, `git stash`로 변경분을 뺀 origin/main 기준선에서 **동일하게 재현**돼(2026-07-27 노트와 같은 사전 존재·샌드박스 네트워크 이슈) 이번 변경과 무관함을 확인했다.
  - ⚠️ **미검증 — 실제 버튼 탭/R키 → 발동 → 무적의 라이브 인터랙션.** 텔레포트 없이(원칙 준수) DEV `__pump`+고정 dt로 실제 걸어서 접근해 검증을 시도했으나, 이 스크립트엔 BFS 길찾기가 없어 벽에 막혀 목표 처치 수(4)를 채우지 못했다(단순 벡터 추적으로 260틱 진행해도 순 이동 9.6유닛 — 미로 지형이 직선 접근을 자주 막는다는 뜻). simBot 수준의 BFS 이동을 새로 짜 넣는 건 이번 세션 범위를 넘어선다고 판단해 멈췄다 — **찰나·몬스터 하우스 버프도 같은 이유로 07-27에 미검증으로 남겼던 전례**를 그대로 따른다. 대신 ①타입 수준에서 게이지 트리거 로직이 실제 처치 게이지 증가 코드 경로(플레이어 이동·DEV enemiesPos·killEnemy 훅)와 맞물려 있음을 코드로 확인 ②DEV 세션에서 던전이 실제로 부팅되고 플레이어가 실제 키 입력으로 이동하며 `__d100.state().enemiesPos`가 실제 적 좌표를 돌려주는 것까지는 라이브로 확인했다. 다음 세션에서 simBot에 결의 버튼 트리거를 추가해 실측할 것(찰나 후속 작업과 같이 묶어도 된다).

### 2026-07-27 — 멀미 신고 2건: 카메라 셰이크 폐기 · 갈림길을 붉은 포털로

플레이 피드백 두 개("몬스터 싸울 때 카메라가 흔들려 멀미가 난다", "한 층 내려갈 때 내려가기 화면이 한 번씩 더 나와 깜빡인다")를 각각 **원인까지 재현해 확인한 뒤** 고쳤다.

- **카메라 셰이크 → 임팩트 파문**(`ripple()`, DungeonScene·GemArenaScene 양쪽). 셰이크는 *한 방짜리* 장치인데 이 게임은 **자동 발사**라 명중이 끊이지 않는다 — 평타 명중마다 `shake += 0.22`가 쌓여 전투 내내 시점이 진동했다. 그래서 무게를 **시점 이동이 아니라 세계의 반응**으로 옮겼다: 맞은 자리에 바닥 충격 링(additive 링 인스턴스 14개)이 퍼지고, 기존 히트스톱은 그대로 둔다(dt=0은 화면을 움직이지 않는다). ⚠️ **부수 효과가 아니라 이득**: 셰이크는 화면 전체라 '어디서 터졌는지' 정보가 없었는데, 파문은 좌표가 있어 때린 자리(적)·맞은 자리(내 발밑)가 색과 위치로 구분된다 — 그래서 `impact()`의 인자도 *방향 벡터*에서 *좌표*로 바꿨다. 검증: `__d100.ripple()` DEV 훅 + `__d100shot()` 픽셀 diff — 파문 프레임은 **플레이어 중심 반경 57~70px에 변화가 몰리고**(대조군은 중앙값 598px = 화면 가장자리 파티클), 전체 변화 픽셀이 92만 중 1.9천(0.2%) = **카메라가 안 움직였다는 증거**.
- **갈림길 화면 → 출구 곁 붉은 포털**. 프레임 단위로 화면 전환을 기록해 1~9층을 실제로 걸어서 확인한 결과 **중복 렌더 버그는 없었고**, 그 화면이 뜬 층은 정확히 갈림길(층 시드 35%)이 열린 층뿐이었다. 문제는 렌더가 아니라 **의미**였다 — 2026-07-26에 없앤 '내려갈래?' 확인창과 생김새·문구가 똑같아서, 가끔 뜨면 사용자에겐 *지워졌어야 할 화면이 되살아난 것*으로 읽힌다. 그래서 선택을 화면에서 **던전 바닥으로** 내렸다: `forkSpot()`이 출구에서 3~5칸 떨어진 설 수 있는 칸을 결정적으로 고르고, 거기 붉은 포털 + 발밑 경고 링이 선다. **어느 포털에 몸을 넣는지가 곧 대답** — 이 게임의 다른 선택(틈·보석·코인 무더기)과 같은 문법이다.
  - 판정 반경은 보통 포털 1.3보다 좁은 **1.1**, 거리는 **3~5칸**. 스쳐서 사나운 층으로 끌려가면 그건 선택이 아니라 사고다.
  - 딸려 나온 삭제: `portal` phase · `PortalScreen` · `stayOnFloor` · `portalRetryRef`와 씬의 재무장 배선(`portalWaitLeave`). 거절할 수 없게 됐으니 '벗어나면 다시 무장' 로직도 존재 이유가 없다.
  - **난수를 한 번도 안 쓴다** — `forkSpot`은 이미 만들어진 층을 훑기만 하므로 층 시드 소비 순서가 그대로고, 기존 층 구조가 한 칸도 안 바뀐다(동굴 층이 지킨 것과 같은 규칙).
  - floor-check에 6번째 불변식 추가(바닥 위·도달 가능·canStand·출구에서 3~5칸): **300개 층 전부 자리를 잡았다**.
- **마을 던전 입구도 같은 규칙**(추가 지시) — 아치에 몸을 넣으면 확인 없이 내려간다. `visit`/`death`는 화면이 아예 없고(`entranceOptions`의 그 분기는 빈 스크립트로), `enter`만 던전 종류 3택이 뜬다 — 그건 '들어갈까?'(확인)가 아니라 **어떤 던전인가**(난이도)라서 없애면 고를 자리 자체가 사라진다. 하단 상호작용 버튼을 눌러도 같은 경로를 탄다(버튼은 발견용으로 남김 — 시뮬봇도 이 버튼을 쓴다).
  - ⚠️ 자동 발동은 **대화 중엔 끈다**: 입구에서 연 선택지를 닫는 순간 같은 자리라 곧바로 다시 열리는 무한 루프가 된다. 시연도 `autoEnter=false`(동선을 드라이버가 직접 잡는데 산책 중 끌려 들어가면 투어가 깨진다).
- **검증**: typecheck · build · floor-check(300층) · assets-check(문서 표 불일치 1건은 **Windows 체크아웃의 CRLF 때문**이라 CI/Linux엔 영향 없음 — 커밋된 파일은 LF) · smoke(콘솔 에러 0, 「포털→드래프트」 구간 포함) · **balance-regression 2판 통과**(사망 6·8 중앙값 8, 기준선 5~13 안 · stuck 0) — 봇은 출구로만 가므로 붉은 포털을 안 밟는다.
- **미검증**: 실기기에서의 체감(멀미가 실제로 가셨는지)은 사람이 봐야 안다. 붉은 포털을 **몰라서 못 보고 지나치는지**(발견율)도 실플레이 관찰이 필요 — 안 보이면 나침반 화살표를 하나 더 다는 게 다음 후보다.

### 2026-07-27 — 리소스 규칙 완화 + 그 규칙을 지키는 장치

**외부 에셋 전면 금지 → 라이선스가 허용하면 반입 가능**으로 규칙을 바꿨다(플레이북 v1.14.0과 정렬). 바뀐 건 금지의 유무가 아니라 방향이다 — 금지가 아니라 **원장 없는 반입 금지**. 절차 생성은 여전히 기본값인데, 그건 취향이 아니라 **복잡성이 파일이 아니라 코드에 있어야 봇·스모크·야간 회귀가 손댈 수 있는 범위가 유지되기 때문**이다.

- **원장 = `docs/assets.json`(기계가 읽는 원본), 문서 = `docs/ASSETS.md`(생성물)**. 처음엔 마크다운 안에 기계 판독 블록을 두는 반대 구조로 만들었는데, 그러면 사람용 표와 기계용 목록을 **둘 다 갱신**해야 해서 결국 낡는다. `npm run assets-write`로 표를 생성하고, `assets-check`가 둘이 어긋나면 실패시킨다.
- **`npm run assets-check`** — ①런타임 바이너리 리소스 ②코드가 실제로 로드하는 외부 호스트 ③직접 의존성 라이선스 ④문서-원장 일치. 배포 게이트(typecheck 다음)에 편입. **위반을 주입해 실제로 실패하는 것까지 확인**했다(통과만 하는 게이트는 죽은 장치).
  - 판별 요령: `<a href>` 하이퍼링크와 `xmlns="http://www.w3.org/2000/svg"`는 **로드가 아니다**. 로드로 세면 게이트가 늘 빨간불이 되고 곧 무시된다.
- **폰트 부팅 로드**(`main.tsx`) — 브라우저는 글자가 실제로 그려질 때만 폰트를 받는데, 이 게임은 문제판·이름표를 **캔버스에 구워 텍스처로** 쓴다. 그때 폰트가 없으면 폴백 서체가 텍스처에 박혀 영영 안 바뀐다. `document.fonts.load` + **1.2초 상한**(CDN이 막힌 사내망·CI에서 부팅이 막히면 더 나쁘다). 실측: 폰트 CDN 차단 상태에서 타이틀까지 459ms.
  - ⚠️ `document.fonts.check('16px Jua')`는 이 검증에 못 쓴다 — CSS가 아예 안 실리면 미지 패밀리를 '가용'으로 보고 **true**를 돌려준다.
- **스모크에 오디오 노드 게이트**(플레이북 9.2) — 소리는 헤드리스에서 들을 수 없으니 **노드를 센다**. 페이지 스크립트보다 먼저 `AudioContext.prototype`을 감싸 `createOscillator`/`createBufferSource`/`createDynamicsCompressor` 호출을 세고, 1층 도달 시점에 **컴프레서 정확히 1개**(마스터 버스가 하나로 수렴했나)와 **소리가 실제로 났나**를 단언한다. 지난 세션에 손으로 확인했던 것이라 회귀를 못 잡던 자리다. 세는 대상은 '구현'이 아니라 **약속**이어야 한다(레시피를 바꿔도 참인 명제).
- 문서 정리: 이 CLAUDE.md의 완료 노트 더미(한 줄에 3000자짜리 13개)를 **날짜별 소제목으로 쪼개 문서 뒤로** 옮기고 앞에 「이 문서 읽는 법」 표를 뒀다 — 규칙·구조가 먼저, 역사는 뒤. 옮길 때 본문은 손으로 옮기지 않고 **스크립트로 기계 이동**한 뒤 글자 수·꼬리 문자열 대조로 유실 0을 확인했다.

### 2026-07-27 — 3종 라이브 세션 즉시 구현 (찰나·좀먹은 바닥·몬스터 하우스 임시 강화)

**찰나(시간 동결)**(`DungeonScene.tsx` — 처치 6회마다 게이지가 차고, 발밑 ❄️ 버튼(또는 F키) 탭으로 1.1초간 **적·보스·탄막만** 세계 시간이 멎고 주인공은 자유롭게 움직인다. 구현: 적 AI·보스·탄막 루프 전체를 `{ const dt = frozenNow ? 0 : worldDt; ... }` 블록으로 감싸 그 구간의 `dt`만 가리는 방식 — 블록 밖(플레이어 이동·차지 샷 타이머·히트스톱)은 바깥 dt를 그대로 써서 전혀 안 건드린다. 접촉 피해 3곳(적·보스·탄막)엔 `!frozenNow` 가드를 추가로 걸었다 — 안 그러면 얼어붙은 채 hitCd만 안 줄어 겹쳐 있는 동안 매 프레임 피해가 들어가는 사고가 난다. simBot은 이 버튼을 누르지 않으므로(DOM 클릭 대상 밖) 기존 밸런스 기준선엔 영향 없음. 파밍이 아니라 처치 게이지를 자원으로 삼아 백층 던전의 전투 리듬에 맞췄다) · **좀먹은 바닥**(`dungeon.ts` `mire` 필드 — 5층+ 22%, 중간 방 하나에 책벌레가 갉아먹은 함정 타일 패치(내부 칸의 ~32%)를 흩뿌린다. 눈에 보이게 어둡게 칠해(`DungeonScene` 바닥 색 레이어에 `mireTint` lerp) "무늬 사이로 피해 갈까, 그냥 밟고 지나갈까"를 실시간으로 판단하게 한다 — **원래 구상한 '숨은 정보(지뢰찾기)' 대신 '보이는 위험'으로 의도적으로 낮췄다**: 빠른 트윈스틱 액션에서 완전히 숨긴 함정은 전략이 아니라 운이 된다. 칸마다 1회만 발동(밟으면 소진), 방 내부에만 놓여 이동 차단이 없으므로 **floor-check의 도달 가능성 불변식과 원천적으로 무관** — 그래도 `scripts/floor-check.mjs`에 벽 속 배치·도달 가능성 검사를 추가해 300개 층 전수 통과 확인. 실시간 액션에 맞게 정보 은닉을 덜어내고 '읽고 피하는' 쪽으로 절충했다) · **몬스터 하우스 임시 강화**(2026-07-25에 만든 아레나 버프(`arenaBuffs.ts`)를 던전 몬스터 하우스로 이식 — 코인 무더기 1·2번째를 주우면 아레나와 똑같이 2택 1, 다만 「이번 아레나 한정」이 아니라 **「이번 층 한정」**(층을 넘으면 `useEffect([floorNo, runId])`가 초기화). `houseStatsRef`가 아레나의 `arenaStatsRef`와 같은 패턴(본체 stats에 버프를 얹은 **사본**만 DungeonScene에 넘김 — 빌드·도감 불변), 새 phase `housebuff`는 GemArenaScene과 달리 DungeonScene을 언마운트하지 않으므로(Canvas가 phase 무관 상시 마운트) 아레나처럼 별도 pausedRef 없이 기존 `phase !== 'run'` 조건 그대로 재사용. `ArenaBuffScreen`에 `variant` prop을 얹어 문구만 갈아 끼움. "그 판 한정 강화"를 새 시스템이 아니라 기존 아레나 버프의 확장으로 재적용했다). **검증**: typecheck·build·floor-check(300개 층, mire 불변식 포함) 통과, 스모크는 이 세션 코드 변경과 무관한 사전 존재 이슈(Google Fonts CDN이 샌드박스 네트워크에서 차단 — 변경 전 origin/main 기준선에서도 동일 실패 재현해 확인) 1건 제외 전부 통과, 별도 100개 층 헤드리스 순회(`__d100app.jump`+`__d100.state().freeze`)로 크래시 없음 확인. **미검증**: 찰나 버튼 실제 탭 → 발동, 몬스터 하우스 코인 실제 주움 → 버프 선택 화면의 라이브 인터랙션(자동화 스크립트로 좌표 이동시키는 대신 실제 걸어서 접근하는 시뮬봇 통합은 다음 작업으로 남김 — 전투 관련 검증에 텔레포트를 쓰지 않는다는 원칙과 충돌하지 않게 하려면 봇 이동 로직이 필요하다).

### 2026-07-26 — 타격감·긴장 봉우리

**히트스톱**(명중 프레임에 dt=0 — 타격 0.22/치명타 0.55/처치 0.4/정예 0.9. **고정 dt(봇 검증) 중에는 끈다**) · **방향성 셰이크**(~~`shakeDir`~~ — **2026-07-27 폐기**, 아래 참조. 멀미 때문에 카메라 이동을 전부 걷어내고 임팩트 파문으로 교체했다) · **차지 샷**(`useChargeInput` — 데스크톱 Space / 모바일 ⚡ 버튼. 누르면 평타를 멈추고 0~1 축적(완충 0.75s), 놓으면 피해 1.5~4배·관통 +2·사거리 1.3배. 0.35 미만은 오조작으로 흘림. **자동 발사는 그대로라 몰라도 예전처럼 플레이 가능**한 선택적 레이어 = 한 손 조작 접근성 유지. 발밑 차지 링, 완충 시 맥동) · **「마지막 문단」**(8층+ 비보스, 출구 7유닛 진입 시 층당 1회 — 5초+층/16 동안 적 광폭화(속도 ×1.45·받는 피해 ×1.5) + **증원 절반은 출구, 절반은 주인공 주위 7~9유닛에 포위 스폰**(5+층/6, 최대 12 — 예비 슬롯 12개 선할당 필수: InstancedMesh 개수가 마운트 시 고정이라 붐비는 층에선 증원이 통째로 안 붙는다). 그동안 **포털이 잠긴다**. `.surge-banner`+`.surge-vignette`, DEV state `surge{state,t}`) · **포털 즉시 하강**(걸어 들어간 게 곧 의도 — 예/아니오를 없앴다. 갈림길 층은 이때 화면을 남겼는데, 그게 2026-07-27에 '버그처럼 읽힌다'는 신고로 이어져 붉은 포털로 옮겨졌다) · **몬스터 실루엣 기능별 분화**(축을 둘로: **층 티어 = 몸통·색 / 타입 = 붙는 기능 부위**. 슈터=포신+외눈, 대셔=쐐기+지느러미, 탱커=어깨+등판, 수문장=뿔+어깨, 체이서=작은 뿔. 타입별 InstancedMesh 5개로 나눠 프레임마다 count 조절. 적이 진행 방향을 바라보게 회전 — 포신·쐐기가 '앞'을 가져야 기능이 읽힌다. ⚠️ three 다면체는 non-indexed, Box·Cylinder는 indexed라 **병합 전 전부 non-indexed로 맞춰야** 한다(안 그러면 씬이 아예 안 뜬다). ⚠️ 크기는 씬 루프의 타입별 스케일이 이미 정하므로 지오메트리에서 또 줄이면 이중 적용된다)

### 2026-07-26 — 사운드 패스, 플레이북 §5 대조

**마스터 버스**(효과음+BGM → Gain 0.9 → 컴프레서 → destination. 겹칠 때 지직거림 해소) · **`noise(q)`**(밴드패스 첨예도 — 히트 날카롭게/피격 둔탁하게) · **차지 샷에 소리를 붙였다**(코어 메커닉인데 사실상 무음이었다 — 방출이 문 통과 징글 `pass()` 재활용이라 '전투의 한 방'으로 안 들렸다): 누르는 동안 **허밍이 차오르고**(chargeHum) → **완충 신호음**(chargeReady — 전투 중 시선은 무리에 가 있어 발밑 링을 못 본다. 놓을 때를 귀로 잡게) → **방출 굉음**(blast(power), 세기 비례) · **치명타 전용음**(crit — 히트스톱 0.55가 화면을 얼리는 순간 소리도 도드라져야 한 방이 무겁게 읽힌다. 던전·아레나 양쪽) · **역류 마지막 5초 초침**(countdown, 3초 이하는 높은 음 — 남은 시간을 바에서 확인하려면 도망치는 화면에서 시선을 떼야 하므로 압박을 귀로 옮겼다) · **`__d100shot()` 스크린샷 회수 훅**(플레이북 §9의 마지막 미구현 항목). 검증: 26개 레시피 전수 호출 + 허밍 수명주기(start/set/stop/중복 stop) 무예외, 버스 AnalyserNode RMS로 효과음·BGM 양쪽 경유 확인, 실주행 차지 0→1.00 완충·해제, 스모크 통과.

### 2026-07-26 — 「마지막 문단」 실측 — 사람형 봇·캡 20·3판

11번 겪고 **사망 0**, 하지만 **평균 체력 손실 20.3%·최대 72.7%**(개별: 0·5·10·10·11·11·18·18·30·37·**73**%). 즉 *억울한 죽음 없이 긴장만 만드는* 분포로 자리잡았다 — 대부분 체력 1/5을 대가로 치르고, 11번에 한 번은 죽기 직전까지 간다. **가설 기각 기록**: 사망률 0%를 보고 "DPS가 높아 적이 표적일 뿐"이라 추정했으나 계측 결과 틀렸다 — 봉우리는 이미 위협이었고 사망만 안 났을 뿐이다. **사망률만으로 '긴장'을 판단하면 안 된다**는 교훈.

### 2026-07-25 — 3종 (아레나 임시 버프·무너지는 서가·동굴 층) + floor-check

**아레나 웨이브 버프 2택 1**(`src/lib/arenaBuffs.ts` ARENA_BUFFS 10종 + `src/ui/ArenaBuffScreen.tsx` — 몬스터 아레나에서 보석 1·2개째를 주울 때 「이번 아레나 한정」 강화 2택 1. **phase는 'arena' 그대로 두고 오버레이만 얹는다** — phase를 바꾸면 GemArenaScene이 언마운트돼 진행 중이던 아레나가 통째로 날아간다. App이 `arenaStatsRef`(본체 stats에 버프를 얹은 **사본**)를 씬에 넘겨서 본체 stats·build·도감은 하나도 안 변한다 = 나가면 흔적 없이 사라짐. `arenaPausedRef`로 선택 중 무리·탄막 정지(GemArenaScene에 pausedRef prop 신설). 선택지는 (runId,층,arenaTry,보석수) 시드라 재현 가능. 재도전(arenaTry++)하면 버프도 초기화. 아레나 체류 중에 새 결정 지점을 하나 만드는 게 목적) · **무너지는 서가 = 역류**(dungeon.ts `collapse` 7층+ 비보스 20% → phase `collapse`, `src/ui/FloorPrompts.tsx` CollapseScreen. 흔들면 **보물 1개를 지금 받고 16초 카운트다운** 시작 → 제한 시간 안에 출구 도달 시 레어+ 보물 1개 + 코인 30(`.ghost-banner` 배너로만 알림 — **quiz phase로 새면 돌아왔을 때 포털이 이미 'pending'이라 다시 안 물어봐서 갇힌다**), 초과 시 최대 체력 22% 낙석 피해. 받은 보물은 실패해도 회수 안 함(되돌리는 처벌은 UX가 나쁘다). 카운트다운은 **run phase에서만** 흐름(오버레이·미니게임 중 정지 = 억울한 실패 방지), **역류 중에는 미니맵이 출구 주변을 밝힌다**(아직 못 본 출구를 제한 시간에 찾으라는 건 선택이 아니라 운). HUD `RushBar` + `.rush-vignette`. DEV state에 collapseWorld/collapseState. 보물을 쥐는 순간이 아니라 '들고 빠져나가는 것'이 진짜 도전이 되게) · **동굴 층**(`isCaveFloor`/`carveCave` — 이끼(11~20)·서리(41~50)·잿빛(71~80) 밴드에서 50%, 보스·흔적·소녀 층 제외 → 기본 시드 기준 10개 층. 셀룰러 오토마타(초기 벽 56% → 이웃 8칸 중 벽 5개+면 벽, 5회 스무딩) → **4-연결 최대 영역만 남겨 연결성 보장** → 그 안에서 '완전히 열린 사각형'만 방으로 잡아 공용 배치 로직을 한 줄도 안 고침. 넓어진 면적(1.65배)만큼 굴에 적을 흩뿌려 바닥 100칸당 적을 맞춤(동굴 9.77 / 방-복도 10.07). **동굴 판정은 층 시드와 별도 난수 스트림** — 메인 rand를 한 번이라도 쓰면 방-복도 층까지 전부 밀린다. 실측 검증: 기존 180개 층 cells 해시·배치 좌표 100% 일치) · **`npm run floor-check`**(`scripts/floor-check.mjs` — 300개 층(시드 3종)의 도달 가능성·벽 속 배치·canStand·고립 영역·면적당 밀도를 esbuild 번들 + 순수 함수로 1초 안에 검사. 봇이 갇히거나 벽에 상자가 박히는 사고를 커밋 전에 차단) · 도감에 「무너지는 서가」·「깎여 나간 장」 2항목 추가(총 37) · driver.mjs에 `PW_EXECUTABLE` 탈출구(playwright 번들 버전과 컨테이너에 깔린 크로미엄이 어긋날 때 다운로드 없이 실행).

### 2026-07-24 — 재미 확장 3종

**갈림길 포털**(층 시드 35%·다음 층 비보스 시 — 🔥모험의 길: 다음 층 스폰 1.5배·피해 1.25배(HUD 🔥), 대신 그 층 돌파 드래프트 전원 레어+ `draftThree(premium)`. dangerFloor state, 죽음·새 도전 시 초기화, 시뮬봇 라벨 불변) · **보스 패턴 3형**(10층 주기 radial→summon→charge 순환 — summon은 격발 2회마다 죽은 슬롯 3기 부활(인스턴스 수 고정이라 보스층에 예비 12슬롯 선할당), charge는 접근→조준(부들부들)→강돌진→착지 링. `bossVariantOf`, DEV state boss.variant/mode) · **도감 「채워지는 책」**(`src/lib/dex.ts`+`src/ui/DexScreen.tsx`, phase `dex` — 보물 21·몬스터 6·사건 8=35항목, 미발견 ❓/???, 수집률 마일스톤 25/50/75/100% 1회성 코인 자동 지급(`d100-dex`/`d100-dex-claim`), 모든 지급·조우 경로에 dexAdd 훅, 타이틀 '📖 채워지는 책 N%' 버튼). **rafshim에 offsetWidth/Height 폴백 추가**(Canvas offsetSize 측정이 0×0 헤드리스 페인에서 r3f 부팅을 막던 것 해소).

### 2026-07-24 — 진화 인게임 연출 + 자동 시연 길찾기

**진화 인게임 이펙트** — 픽 순간 씬 대형 연출(금-장미 이중 폭발 + 황금 잔광 2.6s + 셰이크 + 적 자비 1.2s, `evoFxRef` 시그널) + **발밑 금빛 룬 링 상시 표시**(보유 수만큼 크고 밝게, evoRingRef) + 부채꼴 발사 순간 골드 스파크·확인음. **자동 시연 개선** — ①진화 장면 추가(DemoActions `evolve()`: 멀티샷2+연사2 지급 후 드래프트 직접 오픈 → `.card.evo` 클릭 → 9연발 행진) ②**BFS 길찾기 행진**(`src/lib/path.ts` findPathCells — 프로덕션 포함 순수 함수, DemoActions `nav()`가 미니맵 채널 스냅샷 제공, `marchToExit()`가 경로 추종 키 입력 — 던전 장면의 무작위 wander 전부 대체, 보스층은 출구=보스라 곧장 보스전 직행. 마을·아레나는 산책 유지). ⚠️ 시연 전체 재생은 여전히 실기기 확인 필요(헤드리스 setTimeout 스로틀).

### 2026-07-24 — 진화 「합본」 5종

**진화 「합본」 5종**(`upgrades.ts` EVOLUTIONS — 조합 달성 시 다음 드래프트 첫 슬롯 확정 등장, 판당 1회, 랜덤 풀 비포함. 🌊쏟아지는 문장=4번째 공격 부채꼴 9연발 / 📄종이 표창=벽 1회 반사 / ⭕마침표=치명타 대폭발 / 📕단단한 장정=피격 충격파 / 💰인세=코인 획득 시 +2 회복. Stats에 fanEvery/bounce/critBoom/shockwave/royalty, 던전·아레나 양쪽 반영, `.card.evo` 금-장미 연출, DEV `__d100app.give(id)`로 조합 검증. 설계 검토에서 "빌드가 플레이 방식을 바꾸는 잿팟 순간"이 최대 공백이라 도입).

### 2026-07-24 — 심야 — 자동화 4종, "AI가 만들고 AI가 밤마다 검증"

**부팅 스모크 CI**(`scripts/smoke.mjs` + deploy-pages.yml build job 편입 — ①프로덕션 프리뷰에서 첫 방문자 동선(타이틀 4메뉴→⚡빠른 시작→1층 HUD·코치마크·사서 칩) ②개발 서버에서 DEV 훅 심층(진화 조합 give→포털→드래프트 `.card.evo` 확정 슬롯 + `ghostDist` 중앙값 6~11 검사). 콘솔 error/pageerror 1건이라도 있으면 **배포 중단**. 파비콘류 404만 무시(`scripts/lib/driver.mjs` collectErrors). 로컬 `npm run smoke`는 설치된 Chrome 채널 재사용 — 다운로드 없음. CI 실측 1분대) · **🤖 사서 실주행 파이프라인**(`scripts/daily-ghost.mjs` + `.github/workflows/daily-ghost.yml` 매일 00:15 KST — simBot `daily: true` 옵션(타이틀 「오늘의 던전」 클릭 입장)으로 **오늘의 던전을 실제 봇이 1판 플레이** → `public/ghost/<KST날짜>.json` 커밋(멱등 — 있으면 생략) → **같은 워크플로가 빌드·Pages 배포까지 직접**(GITHUB_TOKEN 푸시는 다른 워크플로를 안 깨우므로). 컨텍스트 `timezoneId: Asia/Seoul`로 페이지 todayKey=KST 보장 + `d100-daily` 날짜 대조 검증, stuck이면 기록 안 씀. **시뮬봇 프레시 프로필 대응**(CI 러너 필수, 로컬 실측으로 발견): ①타이틀·인트로를 범용 big-btn 클릭보다 먼저 명시 처리(6.7/6.8 — 예전엔 '모험 시작'을 눌러 인트로 책 퀴즈 잠금에 영원히 갇힘) ②run phase에서 `__d100` 훅이 없으면 resize 킥(봇 tick 루프가 RO 폴링 타이머를 굶겨 r3f 캔버스 측정이 안 옴 — 마을 분기와 동일 워크어라운드). 실측: 헤드리스 펌핑 주행으로 일일 1판이 약 15초. App은 일일 던전 시작 시 `ghost/<날짜>.json` fetch(정적 파일 — 백엔드 없음 유지) 성공 시 모델 대신 실기록(`ghostReal`→`ghostShown`), 404·JSON 아님이면 조용히 모델 폴백) · **⚖️ 야간 밸런스 회귀 리그**(`scripts/balance-regression.mjs` + balance-night.yml 매일 03:30 KST — 하드런 5판 사망 층 분포를 `scripts/balance-baseline.json`(2026-07-19 실측 6·6·9·9·10·10)과 비교, 중앙값 5~13 밖·3층 미만 사망·stuck이면 **드리프트 → GitHub 이슈 자동 생성**(기존 열린 이슈엔 코멘트), 리포트는 아티팩트+스텝 서머리. 로컬 `RUNS=2 npm run balance-regression`. **CI 실측(2026-07-25)**: 기본 설정은 SwiftShader 렌더 병목으로 1판 ~30분 → `gamePage`(뷰포트 640×360 + lite 강제, 렌더 전용이라 밸런스 불변)로 **5판 16분** — 사망 6·6·10·10·10(중앙값 10), 기준선 안 확인) · **개발 일지 데이터 자동 생성**(`scripts/journal-data.mjs` — git log --numstat 분석으로 journal.html의 `[journal-data]` 마커 블록(days/ledgerAll/cum)과 상단 통계 타일·본문 커밋 수·메타 날짜 범위를 재작성. 검증: 기존 수기 58커밋 구간 값과 100% 일치 + **수기 시절 낡아 있던 07-22(6→14커밋)를 자동 정정**. milestones/categories는 수동 큐레이션 유지 — 참고용 키워드 집계만 출력).

### 2026-07-24 — 밤 — 첫 5분 패스 + AI 사서

**⚡타이틀 빠른 시작**(TitleScreen 메뉴 2번째 「⚡ 바로 던전으로」 — key 교체식 서브메뉴로 모드 3택 후 즉시 `enterDungeon(m)`, 인트로·마을 생략. 첫 방문자·심사자가 30초 안에 전투를 보게. 메뉴가 4항목이 되어 숫자키 1~4 전부 사용, 시연 버튼은 이제 **네 번째**) · **1층 코치마크**(`.coach-chip` — 1층 run 진입 시 평생 1회 9초, `d100-coach`, 시연 중 억제) · **드래프트 진화 힌트 칩**(`Evolution.need` 함수 → **`req` 데이터**(id별 필요 장수)로 구조 변경, `evoHintFor(u, build)` 순수 함수가 '가장 가까운 합본'과 남은 장수 계산 — 재료 카드에 `.evo-hint` 「합본 「X」까지 N장」, 완성 직전엔 `.ready` 금빛 펄스 「집으면 완성!」. 진화 시스템이 첫 드래프트부터 목표로 보인다) · **🤖 AI 사서 고스트**(`src/lib/ghost.ts` — 시뮬봇 하드런 실측(평균 8.3층)에 보정한 추상 층별 시뮬레이션(성장 vs 위협 램프·보스/수문장 스파이크), 2만 시드 분포 p10 4·중앙 8·p90 13·최대 27. 판마다 시드, **일일 던전은 날짜 시드 = 모두 같은 사서와 경쟁**. HUD 층 칩에 `· 🤖N`(.ghost-mark, 추월 시 금색 ✓), 추월 순간 `.ghost-banner` 토스트+unlock음(판당 1회, ghostNotice 4.2s 자동 소거), OverScreen 비교 줄(`.ghost-line`, 사망 사유 GHOST_CAUSE_TEXT), 공유 카드 「🤖 AI 사서」 행. 상수 재튜닝 시 DEV `__d100app.ghostDist(n)`으로 분포 재측정) · **잉크 리빌**(DungeonScene `revealRef`+`writeReveal` — 층 지형(바닥·벽 인스턴스)이 ε 크기로 시작해 **조작 가능해진 첫 프레임부터** 입구→먼 곳 순서로 0.3s씩 자라남, 시차 폭 0.62s. 시각 전용: 충돌·AI·미니맵·봇은 셀 그리드 기반이라 불변. 벽은 밑면 고정으로 자라 s=1에서 원래 배치와 정확히 일치. lite 모드 포함 전 품질 공통).

### 2026-07-24 — 강제 가로 모드 (시스템 회전 잠금 무시)

**강제 가로 모드 — 시스템 회전 잠금 무시**(`src/lib/rotate.ts`): 안드로이드에서 화면 자동 회전이 꺼져 있어도 기기를 눕히면 게임이 돌아간다. devicemotion 중력 벡터로 물리 방향 감지(φ=atan2(-gx,gy), 히스테리시스 35°/55°·10샘플 연속 일치·평면 중력 4.5 미만 무시 = 책상에 눕히면 유지, 거꾸로 180°는 미지원) → App이 `.app`에 `force-cw`/`force-ccw` 부여(뷰포트는 세로인 채 UI만 CSS 90° 회전: width/height 100dvh/100dvw 스왑 + translate+rotate, 회전된 .app이 내부 fixed의 containing block이라 steer-zone도 함께 돎). **Canvas는 `resize={{ offsetSize: true }}` 필수** — gBCR은 회전된 bbox(세로)를 돌려줘 r3f(react-use-measure)가 캔버스를 잘못 재기 때문(레이아웃 offsetWidth로 측정). 입력 보정: useMoveInput 드래그 벡터·useSteer 좌/우 판정이 `appRotation()`(0/90/-90)으로 뷰포트→화면 축 변환(90: (dx,dy)→(dy,-dx) / -90: (-dy,dx)). 뷰포트가 이미 가로면(잠금 해제·데스크톱) 항상 해제, resize 시 즉시 원복. iOS는 센서 권한 팝업이 필요해 자동 활성화 안 됨(조용히 비활성). `.vstick`은 body 소속(뷰포트 고정)이라 회전과 무관하게 손가락 위치에 맞음. 헤드리스 검증: `new DeviceMotionEvent('devicemotion', {accelerationIncludingGravity})` 합성 이벤트로 발동 확인.

### 2026-07-23 — 모바일 가로/세로 + 가상 조이스틱

**모바일 가로/세로 + 가상 조이스틱** — ①**보이는 가상 조이스틱**(`.vstick`/`.vstick-knob`): useMoveInput에 내장(모듈 싱글턴 DOM — 던전이 숨겨진 채 아레나 훅과 공존해도 1개), 터치·펜 드래그 시 손가락 위치에 표시되고 반경(STICK_R=56, len/56 정규화와 일치)을 넘겨 끌면 베이스가 따라오는 플로팅 스틱. 마우스는 시각화 없이 예전 동작. `useMoveInput(pausedRef?)` — 던전·마을은 pausedRef를 넘겨 미니게임·대화 중 표시 억제, 스틱 시작은 **캔버스 위 + 버튼 밖**에서만(오버레이 드래그로 안 뜸) ②**두 문 달리기 존 인디케이터**(`.steer-zone` ◀▶, `(pointer: coarse)` 기기 전용, pointer-events 없음 — 실제 입력은 여전히 화면 반쪽 어디든, 누른 쪽 `.active` 하이라이트) ③**가로/세로 대응**: `100dvh`(@supports — 모바일 주소창·회전 높이 변동), 노치 safe-area 좌/우(`.hud`/`.mini-map`/`.build-row`/`.title-mute`/`.title-gfx`/`.demo-exit`/`.screen` 패딩), `.screen`에 `overflow-y:auto`+`touch-action:pan-y`+`justify-content: safe center`(내용이 화면보다 길면 스크롤), 낮은 화면 압축은 `:root.low-h` 클래스(h1·버튼·카드·대화창 축소 — App이 max-height 520px 매치 or 강제 회전 시 부여), `.doorrun-hint` max-width 말줄임.

### 2026-07-22 — 그래픽·UI 품질 패스

**그래픽·UI 품질 패스** — 블룸·비네트 포스트프로세싱 + 블롭 섀도우 + 가짜 AO + 절차 돌결 텍스처 + Hero 주스 + 투사체 스트레치·additive 파티클 + UI '책' 디자인 언어·잉크 전환·타이틀 연출 (전부 생성 에셋, 상세는 DESIGN.md). **주인공 변신**(던전 종류별 초등학생/대학생/모험가). **떠돌이 상인 부활**(🎩 — 비주얼노벨 시절 원본 대사 복원, visit/death 마을에만 등장(바퀴 없는 마차와 함께), 첫 만남 독백+덤 아이템(`d100-peddler`), 코인으로 이번 판 아이템 구매: (runId,층) 시드 매대 3종 방문당 고정·희귀도별 가격 40/70/120 — 대장간=영구 vs 상인=즉석 화력 경제 분리, TownNode choice에 `buy` 액션). **미니맵**(`src/ui/MiniMap.tsx` — DungeonScene이 채널 ref에 좌표·탐사 마스크를 쓰고 DOM 캔버스가 150ms 간격으로 그림, 탐사한 곳만 밝혀지고 포털·상자·마을 문·붉은 포털 마커도 발견 후 표시, z 20이라 오버레이 아래). **처치 콤보**(3초 창 연속 처치 4+ 코인 ×2 / 8+ ×3 — 탐욕과 곱연산, `.combo-chip` 하단 중앙). **모바일 진동**(App `buzz()` — 피격 30ms·처치 12ms·보물 40ms(goldFlash 공통)). **그래픽 품질 — 자동 조절 + 토글**(`d100-gfx` pref: auto/high/lite, 기본 auto — ⚡가벼움: 포스트프로세싱 없음·DPR 캡 1.5·바닥/벽 플랫 재질(재질은 key로 통째 교체 — HMR식 prop 잔재 방지), 기존 렌더 경로와 동일 + 공짜인 블롭 섀도우·AO는 유지 / ✨고품질: 블룸·비네트 MSAA 2·텍스처·DPR 2. **auto 판정**: 모바일 UA는 측정 없이 lite, 데스크톱은 high로 시작해 run 중 워밍업 2초 후 3초 창 평균 FPS를 재서 45 미만이면 lite 강등(+`.gfx-notice` 배너)·55 이상이면 high 확정 — 결정은 `d100-gfx-auto`에 저장돼 다음 세션 즉시 적용, 숨김 탭(프레임 250ms+)은 측정 창 리셋. 토글(타이틀 `.title-gfx`·HUD ⚡/✨)을 누르면 수동 설정으로 전환 — 라이브 반영 확인됨). **잉크 전환은 opacity 페이드**(2026-07-22 재작업 — 거대 원 scale 방식이 저사양에서 잭이 나며 '깜빡'처럼 보였음).

### 2026-07-19 — 업그레이드 풀·일일 던전·방 이벤트

업그레이드 풀 16종(희귀도·시너지) · 일일 던전(날짜=시드) · 방 이벤트 3종(제단·몬스터 하우스·찢어진 페이지).

## 관련
- 자매작(원작): 두 문 러너 — https://github.com/Hakhyun-Kim/door-runner. 그 두 문 달리기가 이 게임의 보물상자 미니게임(DoorRunScene)으로 **통합 완료**(2026-07-13).
